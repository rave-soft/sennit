# План рефакторинга

Только открытые пункты. Убирайте пункт, когда он закрыт; история остаётся
в git. Внутри фазы задачи независимы и могут идти параллельными коммитами.

Условные обозначения: **[S]** малая правка (≤ 1 файл, ≤ 1 час),
**[M]** средняя (несколько файлов, тесты), **[L]** структурная (новый пакет
или перенос ответственности).

## Решения первого аудита (2026-09-02, bdc47daab), которые стоит помнить

Все семь фаз закрыты. Три вывода остаются в силе:

**Кеширование окружения конфигурации отклонено, не отложено.** Записи
`env` резолвятся лениво против текущего окружения, и это зафиксировано
двумя тестами: повтор после ошибки авторизации и
`TestLoadRuntimeResolverReadsCurrentEnvironment`. Попытка закешировать
сломала оба. Проблема реальна — запись с `$(cmd)` выполняет команду на
каждое разрешаемое значение — но лечится не кешированием целиком.

**Три места в `internal/workspace` остаются на своих местах.** Перенос
`resolve_session.go`, `session_changes.go` и `custom_provider.go` в
`appws` дал бы цикл импорта или заставил бы UI импортировать бэкенд
напрямую.

**Ранняя проверка границ рабочей папки в мутирующих инструментах не
избыточна.** Без неё сначала срабатывают stat и проверка свежести, отказ
приходит с другим текстом, а файлы вне папки читаются до отказа.

---

# Аудит 2 (2026-09-02, `main`, b04a0e592)

Критерии: SOLID, DRY, KISS, YAGNI, границы контекстов. Каждая находка
подтверждена чтением кода; статистика (граф импортов, длина функций,
плотность комментариев) снята скриптами по `internal/`, без
`third_party`.

## Фаза 1. Баги и подозрительные места

### 1.1 [отклонён] Непроверенный `Close` при скачивании

Находка ошибочна. `errcheck` указывает на `outFile.Close()` в `defer`
(`download.go:187`), но это путь очистки: файл там всё равно удаляется по
`cleanupTmp`. Успешный путь закрывает файл явно и проверяет ошибку
(`download.go:204`) до `fsext.ReplaceFile`. Порядок «скопировать →
закрыть с проверкой → переименовать» уже верный, менять нечего.

## Фаза 2. SOLID

### 2.1 [отклонён] `config.ConfigStore` — god-объект

71 метод — правда. Но пакет не режется, и это проверено двумя разными
гипотезами, а не одной.

**По данным (credentials) — не режется.** Запись креденшела *и есть*
запись конфига: она публикует новый `*Config` под тем же `writeMu`, что
и любой мутатор. Вынесенный тип всё равно принимал `*ConfigStore`
параметром ради восьми чужих полей — два получателя на одну сущность
плюс шесть форвардеров. Откачено; причина записана в шапке
`credential_store.go`.

**По времени жизни (reload/watch/staleness) — тоже не режется.**
Гипотеза была, что эта группа работает с файлами на диске под своим
`reloadMu`, а не с `*Config` под `writeMu`. Неверно: `reloadFromDisk`
берёт `writeMu` на чтение (`reload.go:33,54`) и на полную запись для
финальной подмены (`reload.go:123`), а `staleness.go:200` прямо требует
от вызывающих держать `writeMu`, если они могут гоняться с публикацией.

**Общий вывод, который стоит помнить.** `ConfigStore` — не свалка
несвязанных обязанностей, а один опубликованный снимок с множеством
способов его обновить. Всякий путь, что бы ни делал по дороге,
заканчивается `setConfig` под `writeMu`. Резать по любому признаку —
данные, время жизни — значит оставлять новому типу зависимость от
исходных локов. Пока `*Config` публикуется целиком, 71 метод — это
цена, а не небрежность.

Что тут действительно хорошо сделано и что не стоит сломать: перезагрузка
держит `writeMu` только на подмену, а дисковый ввод-вывод и HTTP
выполняет вне лока — это зафиксировано тестом
`TestReloadFromDiskLocked_DiscoveryDoesNotBlockWriteMu`.

### 2.2 [L, частично закрыт] `agent.delegationFinalizer` делает всё

Skills-состояние и HTTP-клиент fetch-инструмента вынесены в свои типы
(`skillsState`, `fetchClient`); пять мьютексов/атомиков стали три.
Осталось: `subSessions`, `delegationTools`, `runtimeInputsCache` — они
про делегирование по существу, и разрезать их дальше стоит только если
появится причина, а не ради счёта полей.

### 2.3 [закрыт] `ui/model.UI`

Диспетчеры разобраны (`947077b3f`), `keyMapForPlatform` тоже
(`df7377d09`). По самой структуре — дальше без передела не идёт, и это
измерено, а не оценено на глаз.

Из 35 полей 29 уже подструктуры (`sess`, `editor`, `lay`, `sidebar`,
`queued`, `wsCache`, `promptQueue`, `panel`, `lsp`, встроенные `widgets`,
`term`, `mouseState` и прочие). Оставшиеся шесть — `com`, `embedded`,
`focus`, `state`, `keyMap`, `goos` — сквозные по существу: общий handle
и состояние, которое маршрутизирует сообщения и открывает каждый экран.

Подсчёт транзитивного замыкания по группам для каждого метода показал,
что почти все трогают две и больше групп по замыслу: диспетчеры,
отрисовка и обработчики законно нуждаются в `com` плюс состояние.
Однозначно односоставных нашлось четыре, они и переехали на свои
структуры (`dfe8b5758`). Остальное требует передела, а не перекладывания.

### 2.4 [отклонён] `readOnlyWorkspace` написан руками

799 строк и 120 методов, из них 54 форвардят в `w.ws`, — но встраивать
`Workspace` нельзя, и это уже осознанное решение, описанное в шапке файла
(`read_only_workspace.go:45-69`). Без встраивания каждый метод контракта
обязан быть реализован явно, иначе падает `var _ Workspace =
(*readOnlyWorkspace)(nil)`. Это default-deny: новый мутирующий метод
контракта — ошибка компиляции, а не молчаливое проксирование в живой
workspace. Встраивание обменяло бы ошибку компиляции на тихую дыру.
Полноту классификации сторожит
`TestReadOnlyWorkspace_MethodClassificationIsComplete`. Форвардеры —
цена default-deny, а не небрежность.

### 2.5 [закрыт] Контракт `FrontendWorkspace` неоднороден

Десять «голых» методов разложены по ролям (`OAuthController`,
`PreferredModelUpdater`, `ConfigFieldEditor` плюс новые
`ProviderCatalog` и `AccountUsage`); в теле `FrontendWorkspace` не
осталось ни одного. Набор методов контракта не изменился — 118 имён до и
после, ни одна реализация и ни одна тестовая заглушка не тронуты.

**Утверждение аудита про семь однометодных `Account*` было неверным.**
Они не «без потребителей»: все семь берутся в `internal/cmd` как типы
параметров и поля локальных составных интерфейсов —
`recordCopilotAccount(ws workspace.AccountRecorder, …)`,
`findAuthAccount(ws workspace.AccountLister, …)` и так далее. Роль с
настоящим узким потребителем — это то, ради чего роли и заводят;
сворачивать их было бы регрессом. Проверять потребителей **до**, а не
после.

### 2.6 [частично закрыт] `shutdownPhases.Shutdown`

Пять одинаковых циклов по очередям хуков сведены к `runHookList`
(`fc2361e51`). **Таблица фаз отклонена.** Остальная последовательность не
раскладывается на однородные записи: шаг остановки агента ветвится между
тестовым сидом и путём через координатор, параллельный шаг запускает две
горутины со втиснутым между ними закрытием herdr, а `mcpClose`,
`mainDBRelease` и `messages.Close` — одиночные действия под флагами,
вычисленными по ходу. Таблица потребовала бы пяти escape-hatch'ей и
читалась бы хуже явной последовательности. Заодно закрыта дыра в
покрытии: относительный порядок `preCleanupHooks` и `shutdownHooks` не
был зафиксирован ни одним тестом.

## Фаза 4. KISS

### 4.1 [закрыт] Комментарии рассказывают историю багов вместо кода

Пройдены все шесть крупных пакетов: `thread` (17 %), `ui/dialog`,
`ui/chat` (2 %), `agent` (1 %), `config` (1 %), `ui/model`.

**Отношение комментариев к коду — негодная метрика, не опирайтесь на
неё.** Исходная оценка «минус треть» выведена из неё и завышена на
порядок. Три прохода подряд показали одно и то же: большая часть
комментариев несёт рассуждение, которого в коде нет, и целыми файлами
править нечего — в `ui/chat` пятнадцать файлов из тридцати трёх не
потребовали ничего, в `thread` семь из двенадцати, в `agent` тридцать
один. Настоящая цель — историческая наррация, и её на порядок меньше,
чем показывает отношение.

Как это делать, чтобы не навредить:

- **Никаких числовых нормативов.** Первая попытка задала «минус треть»,
  и это немедленно превратилось в вырезание объяснений ради цифры.
  Единственный критерий — восстановима ли информация из кода;
  сомневаешься — оставь. Недорезать дешевле, чем срезать несущее.
- **Ни одной строки кода.** Проверяется командой:
  `git diff -U0 -- <pkg> | grep '^[-+]' | grep -v '^[-+][-+]' | grep -vE '^[-+]\s*(//.*)?$'`
  должна печатать пусто.
- **Не гоняться за подстрокой.** «used to» чаще всего значит
  «используется, чтобы», а «no longer» — состояние рантайма, а не
  прошлое кода. Список исключений выдавать вместе с заданием, иначе
  проход сломает нормальные комментарии.
- Убирать: историю («used to be», «the old code», «before this was
  extracted»), пересказ следующей строки, одно и то же рассуждение у
  хелпера и у каждого его вызова, пять предложений на двухстрочный
  guard, указатели на документы, которых нет в репозитории.
- Оставлять: почему сделан неочевидный выбор, инвариант для вызывающего,
  перекрёстную ссылку на место, где значение читают обратно,
  предупреждение о ловушке, ответ на «почему это экспортировано», и —
  в UI — всё про ширину терминала, ANSI и переносы.
- **Прошлое лекарство переписывать в живое предупреждение.** Не «раньше
  это обходили через `os.Setenv`», а «в отличие от `os.Setenv`, которая
  испортила бы ключ всем остальным провайдерам». Опасность сохраняется,
  рассказ уходит.
- Результат «этот файл уже в порядке» — валидный.

Попутная польза, которую стоит ожидать: проход по `ui/chat` нашёл три
дефекта документации — `MCPToolRenderContext` был описан как рендерер
bash-сообщений, и три doc-строки задублированы склейкой при слиянии.

### 4.2 [закрыт] Длинные функции

Разобраны все: `updateMouse` 347 → 15 и `updateSession` 341 → 37
(`947077b3f`), `summarize` 285 → 33 и `getProviderOptions` 265 → 32
(`e9c88512a`), `QuestionForm.Draw` 251 → 10 (`950de0598`),
`keyMapForPlatform` 250 → 44 (`df7377d09`), `Shutdown` (`fc2361e51`),
`runTurn` 96 → 31 (`0580c1b90`). `scanBackward`
(`agent/tools/sennit_logs.go`, 219) **отклонён** — см. 5.6.

Что оказалось важным при разборе рендерящих и диспетчерских функций:
проверять не «стало короче», а конкретные инварианты. В `Draw` — что
путь confirm-таба по-прежнему возвращает курсор без поправки, а путь
вопроса с ней. В `keyMapForPlatform` — что карта привязок берётся один
раз и `overrides` применяются к ней в той же точке. В диспетчерах UI —
что `break` внутри `case` превращается в `return` только там, где после
него ничего не дописывается в `cmds`. И golden-файлы обязаны проходить
**без** `-update`: расхождение там означает изменение поведения, а не
повод их перегенерировать.

## Фаза 5. YAGNI и границы контекстов

### 5.1 [отклонён] `internal/proto` охраняет дублирование

Находка неверна целиком. Она выведена из числа строк, имени пакета и
наличия файлов `*_parity_test.go` — при поиске потребителей я искал
префикс `proto.` и не увидел главного: **тридцать файлов `internal/ui`
импортируют этот пакет под псевдонимом `tools`**
(`tools "github.com/rave-soft/sennit/internal/proto"`). Именно ради них
существует основная масса пакета: рендерер чата декодирует сохранённый
JSON вызовов инструментов, не втягивая тяжёлый `internal/agent/tools`.

Ни одного неиспользуемого экспортированного символа в пакете нет.
Проверено посимвольно, включая типы, которые нигде не названы по имени
снаружи (`MultiEditOperation`, `FailedEdit`, `QuestionChoice`,
`ReadResourceType`), — каждый является типом поля живого соседа.
Пробные удаления немедленно ломали сборку `internal/ui/chat`.

**Parity-тесты не охраняют дублирование, а проверяют полноту
конверсии.** Их собственный комментарий говорит прямо: копия — это
замысел, потому что `proto` намеренно не зависит от `internal/hooks`.
Тест ловит реальный отказ: поле, добавленное в `hooks.HookInfo` и
забытое в `proto.HookInfo`, — не ошибка компиляции, так как `internal/
agent` копирует поштучно, и транскрипт молча перестаёт показывать то,
что раннер знает. `lsp_parity_test.go` тем же способом ловит новое
состояние LSP, проваливающееся в `default`.

### 5.2 [отклонён как сформулирован] `internal/config` — хаб

Три независимые причины, каждая достаточна сама по себе.

**Посылка неверна.** `Config()` возвращает не стор, а опубликованный
снимок-значение: `ConfigStore.Config()` отдаёт `s.config` под RLock, а
`cloneForWrite` глубоко копирует всё, что трогает мутатор, ровно чтобы
чтение снимка не брало лока. Мьютексы, перезагрузка и путь записи наружу
не видны. Формулировка «UI получает весь стор» ложна.

**Инструмент выбран неверно.** DTO пришлось бы держать в согласии с
пятнадцатью полями `config.Config`, который прирастает полями при каждой
второй фиче, и забытое поле в конвертере не ловится ничем — экран просто
тихо отрисует ноль. Parity-тесты `proto` тут не помогут: они держат
замкнутые перечисления, а не состав структуры. И `AGENTS.md` прямо
запрещает заводить DTO «на всякий случай».

**В UI уже есть правильный приём, и он не DTO.** `internal/ui/chat` не
принимает `*config.Config` вовсе: `ModelConfig` (`messages.go:338`) и
`CustomAgentConfig` (`tools_core.go:250`) — узкие интерфейсы на два-три
метода, которым `*config.Config` удовлетворяет неявно. Продакшн-код
`ui/chat` не импортирует `internal/config` совсем.

**Выигрыша по зависимостям не было бы.** `internal/workspace` называет
`config.Scope` в `SetConfigField`, `RemoveConfigField`, `SetCompactMode`
и `RecordAccount`, а `config.Problem` — в `ConfigProblems`. Замена
одного `Config()` на DTO не убирает `internal/config` и его 28
транзитивных пакетов из линковки TUI. Тот же урок, что и в 5.3.

**Счёт «22 импортёра» — шум.** `lsp` объявляет свой узкий
`ConfigProvider`; `projects` зовёт один хелпер пути; `importer` и
`doctor` используют типы-значения и константы. Ни один не трогает
поведение стора.

Что остаётся сделать, если захочется: завести такие же узкие интерфейсы
в `ui/model` и `ui/dialog`, где сосредоточены почти все 24 обращения.
Это инкапсуляция и читаемость, **не** уменьшение связности — продавать
это как выигрыш по зависимостям нельзя.

**Найдено попутно и стоит помнить:** неизменяемость снимка держится на
соглашении, а не на типах. `Config.Providers` — `*csync.Map`, ссылочный
и мутабельный: держатель `*config.Config` может изменить его на месте в
обход clone-and-swap. Продакшн так не делает,
`ui/dialog/accounts_test.go:387` — делает.

### 5.3 [переформулирован] В контракте `workspace` живут побочные эффекты

Исходная формулировка («импортирует 15 пакетов, `MaxBackgroundJobs`
возвращает `shell` в UI транзитивно») проверку не прошла. Классификация
всех пятнадцати по фактическому использованию:

- **11 — чистые DTO**, ради которых контракт и существует: `message`,
  `session`, `history`, `oauth`, `permission`, `proto`, `question`,
  `skills`, `stats`, `providers/accounts` и `config` (последний тяжёлый,
  но несущий: `config.Scope` принимает почти каждый метод записи).
- **3 — поведение**: `git.UncommittedFiles` запускает подпроцесс
  `git status` (`read_only_workspace.go:355`); `discover.DiscoverModels`
  ходит по HTTP (`custom_provider.go:59-66`); `diff.GenerateDiff` —
  чистое вычисление, безобидно.
- **1 — случайность**: `shell` ради одной константы. Убрано
  (`185b43f14`), значение продублировано с тестом
  `TestMaxBackgroundJobsMatchesShell` против расхождения.

**Транзитивно это ничего не дало, и рассчитывать на такое не стоит.**
`internal/config` сам тянет `internal/shell` через `shellconfig` и
`hooks`, поэтому замыкание контракта — 41 пакет и до, и после, а
`internal/shell` в нём остаётся.

**Подпроцесс убран** (`8bef12925`): `readOnlyWorkspace` получает
`uncommittedFiles` инъекцией, `appws/threads.go` передаёт туда
`git.UncommittedFiles`. Ни один продакшн-файл контракта больше не
запускает подпроцесс. Но это перенос поведения, **не** выигрыш по
зависимостям: `git.FileChange` остаётся в сигнатуре
`Workspace.UncommittedFiles`, замыкание — 41 пакет и до, и после.

**Сетевой вызов остаётся, и это упирается в жёсткое ограничение.**
`ConfigureCustomProvider` зовут напрямую из `ui/model/dialog_actions.go`
с `ws` типа `workspace.Workspace`. Инъекция discovery потребовала бы
метода в `customProviderConfigurer`, а раз вызов идёт через `Workspace`,
метод пришлось бы добавить и туда — то есть расширить контракт ради
сужения. Причина цикла в старой попытке выяснена точно:
`PrepareSessionChangesUsing` зовут и `appws.AppWorkspace`, и
`readOnlyWorkspace`, а последний обязан жить в `internal/workspace`, так
что перенос хелпера в `appws` заставил бы `workspace` импортировать
`appws`.

### 5.4 [частично закрыт] Docker MCP размазан по восьми пакетам

Зонд, TTL-кеш и процессный глобал вынесены в листовой
`internal/dockermcp` (`5976c13ef`), `internal/config/docker_mcp.go` — со
186 строк до 85. Мутаторы стора (`EnableDockerMCP`, `PersistDockerMCPConfig`
и прочие) остались в `config`: им нужны его потроха. `DockerMCPConfig()`
тоже остался — он возвращает `MCPConfig`, тип самого `config`, и переезд
развернул бы стрелку зависимости обратно. Рендер в `ui/chat`, `ui/dialog`
и `ui/model` не тронут — это отдельный пункт, если он вообще нужен.

### 5.5 [отклонён] `internal/agent` смешивает два контекста

Находка выведена из имён файлов и числа строк. Проверка по зависимостям
её не подтвердила: из пяти названных файлов ни один не является
провайдерным кодом, попавшим не в тот пакет.

- `credential_refresh.go` — 10 функций из 10 суть методы
  `*runtimeBuilder`; часть принимает `*activeRuntime` и возвращает
  `*compiledRuntime`.
- `providers_build.go` — 12 из 15 методы `*runtimeBuilder`, а сам
  `runtimeBuilder` тянет `agent/prompt`, `agent/tools`, `hooks`, `lsp`,
  `permission`, `question`, `session/store`, `shell`, `skills`.
- `rotation.go` — все функции нанизаны на `*activeRuntime` и
  `runtimeOperationPort`, который несёт живого агента координатора.
- `provider_log.go` — единственный без ссылок на оркестрацию, но
  неотделим на уровне неэкспортированных символов от `provider_stall.go`:
  `instrumentedModel.Stream` зовёт `newStreamStallWatchdog` и
  `isProviderStall` и читает тамошние константы таймаутов. Перенос
  потребовал бы экспортировать потроха сторожа через границу пакета.
- `compat.go` — вообще не про провайдеров: `preparePrompt`, починка
  осиротевших tool-результатов, напоминание про todo. Провайдерного там
  только `workaroundProviderMediaLimitations` и `cacheControlOptions`.

Импорты строго односторонние: `internal/agent` → `internal/providers/*`,
обратно ничего. Разделение 3.6 уже вынесло всё, что выносится без
затягивания графа оркестрации; оставшееся осталось потому, что ему нужны
`message`, `session/store`, `hooks`, `lsp`, `permission`, `skills` —
ровно то, от чего `internal/providers/*` правильно не зависит.

Единственное, что тут стоит поправить, — имя `compat.go`: файл
разнородный, и его название обещает провайдерные шимы, которых там
почти нет.

### 5.6 [отклонён] Сложность `sennit_logs` оправдана

Находка была выведена из размера файла (1357 строк) без чтения схемы и
тестов. Проверка это не подтвердила:

- каждый параметр `SennitLogsParams` документирован в `sennit_logs.md.tpl`
  и достижим из модели; схема и описание не разошлись;
- `scanBackward` — не редкая ветка, а единственный путь чтения: он
  вызывается на каждом успешном обращении (`sennit_logs.go:300`) и, сверх
  того, переиспользуется вторым инструментом (`agent_trace.go:144,162`);
- курсорная пагинация обещана в `<pagination>` и покрыта именными
  тестами на ротацию, устаревание, границы чанков и дозапись;
- 59 тестовых функций в трёх файлах, причём часть инвариантов
  `scanBackward` закреплена тестами, на которые ссылаются комментарии по
  имени.

Разбор `scanBackward` на именованные шаги тоже отклонён: его 219 строк —
в основном объяснения байтовых смещений и бюджета сканирования, а
дробление ради читаемости рисковало бы корректностью там, где её держат
эти самые комментарии и тесты.

## Что осталось открытым

Аудит отработан. Ниже — только то, что осознанно не сделано, с причиной.

**5.3, сетевой вызов в контракте — закрыт, см. аудит 11.** Запись
ниже устарела: `internal/workspace/custom_provider.go` больше не
импортирует `discover`, обнаружение инжектировано через
`ModelDiscoverer` (`custom_provider.go:42`), HTTP живёт в
`appws/custom_provider.go:24-38`.

**5.4, рендер Docker MCP.** Зонд и кеш вынесены; отрисовка размазана по
`ui/chat`, `ui/dialog`, `ui/model`. Отдельный пакет под неё имеет смысл
только если появится второй такой вендор — сейчас это было бы
обобщение под один случай.

**2.2, остаток `delegationFinalizer`.** `subSessions`,
`delegationTools`, `runtimeInputsCache` — про делегирование по существу.
Резать дальше стоит по причине, а не ради счёта полей.

**`RuntimeProviders` заморожен** — закрыто: `setConfig` замораживает
обе карты.

**`config.NewStore` публикует без заморозки — решено оставить, с
замером.** Асимметрию попробовали снять и откатили. Заморозка в
`NewStore` ломает `TestMakeThresholdRotateCallback_BelowThreshold_NoOp`
и ещё одиннадцать тестов: они строят стор через `configtest` и потом
зовут `SetRuntimeProvider` по опубликованному снимку. Чтобы их
сохранить, потребовалась бы опция `configtest`, задающая runtime-провайдер
до публикации.

Цена известна, а выгоды нет: все продакшен-вызовы `SetRuntimeProvider`
живут внутри `internal/config` и работают с ещё не опубликованным или
клонированным конфигом, а `NewStore` из прода недостижим вовсе —
единственный вызывающий это `configtest`. То есть заморозка здесь не
защищает ни от чего реального, а лишь ограничивает тесты. Возвращаться
к этому стоит только если `NewStore` понадобится в проде — тогда
публикацию проводить через `setConfig`.

**Узкие интерфейсы в `ui/model` и `ui/dialog`.** Там сосредоточены почти
все 24 обращения к `Config()`. `ui/chat` уже показывает образец
(`ModelConfig`, `CustomAgentConfig`). Это читаемость и инкапсуляция, и
продавать это как уменьшение связности нельзя — линковка не изменится.

---

# Аудит 3 (2026-09-03, `main`, c44e3d612)

Проведён по методу, выведенному из провала предыдущего: читать
потребителей, а не считать метрики. Стандарт приёмки находки — сценарий,
путь с `file:line`, последствие и цитаты кода; формулировки вроде
«выглядит хрупко» отклонялись сразу. Четыре области, три находки, все три
пережили перепроверку и закрыты.

Закрыто: незащищённое чтение вне рабочей папки в `glob`/`grep`/ripgrep
(`db2bac955`), потеря завершения припаркованного треда (`5c73b1498`),
живые обновления вложенных делегирований (`0ebb3d633`).

## Открыто после третьего аудита

### A1 [закрыт] Transient-отказ перезагрузки помечает изменение обработанным

`internal/config/watch.go:98-103`: когда `ReloadFromDisk` возвращает
ошибку, сторож всё равно зовёт `CaptureStalenessSnapshot`, а
`statSnapshot` (`staleness.go:67`) берёт за эталон файл **в текущем виде
на диске** — то есть отвергнутую правку. Изменение помечено увиденным,
повтора не будет.

**Осторожно, здесь легко сделать хуже.** Для невалидного конфига текущее
поведение правильное: без capture `reloadFromDisk` — а он делает HTTP за
моделями — запускался бы каждый интервал опроса. И правку пользователя
это не теряет: исправление меняет файл, mtime сдвигается, изменение
детектируется заново.

Дыра только в transient-случае: таймаут discovery, разовый сбой
`ValidateHooks`, ошибка ФС. Файл валиден, правка нужна, но не применится
никогда. Чинить — классификацией ошибки («конфиг плох» против «попытка
сорвалась»), а не удалением capture.

### A2 [закрыт] Сброс сообщений при завершении не повторяется

`internal/message/store/service.go:414-429`: `Close` выставляет
`s.closed = true` **до** `FlushAll`, а `rearmFlushTimer` (`:567`) при
`closed` гарантированно no-op. Значит любой отказ записи во время
собственного сброса `Close` неповторим. Ошибка доходит до
`app/shutdown.go:321` и логируется, но без указания, какое сообщение
потеряно. Узко — процесс всё равно завершается, — но лог стоит сделать
адресным.

### A3 [закрыт] `Config.AgentOverride` не nil-безопасен

`config.go:810` обращается к `c.Agents` без проверки `c == nil`, тогда
как восемь его соседей в том же файле такую проверку имеют. В проде
недостижимо: стор всегда публикует непустой конфиг. Находкой не
считается — записано как несогласованность, которая ждёт первого
тестового двойника, возвращающего `nil`.

---

# Аудит 4 (2026-09-03, `main`, b0975aeea)

Тем же методом, по областям, которых не касались первые три: `lsp`,
`shell`, `db`, `mcp`, `oauth`, `hooks`, `skills`. Выбор области был не
случаен — оба падения CI после первого пуша пришли именно оттуда.
Четыре находки, три закрыты.

Закрыто: молчаливая порча файла в `applyTextEdit` (`3025624a3`),
`StateNeedsAuth` не чистил сессию и каталог MCP (`54841f0b5`), ложный
комментарий про Windows (`9364e2c87`). Плюс два теста, которые CI нашёл
раньше меня: гоночный тест сторожа скиллов (`a651a4329`) и SIGINT-тест,
неприменимый на Windows (`b0975aeea`).

## Открыто после четвёртого аудита

### B1 [L, не делать наспех] Windows не убивает дерево процессов

`interp.DefaultExecHandler`, который `internal/shell/exec_windows.go`
использует без изменений, не ставит `SysProcAttr` вообще, шлёт сигнал
одному процессу и не задаёт `WaitDelay`. Внуки переживают отмену
команды; `Wait()` может зависнуть, пока внук держит открытым пайп
вывода. На Unix ровно это закрыто через `Setsid` и убийство группы по
отрицательному PID.

Комментарий, обещавший покрытие через `CREATE_NEW_PROCESS_GROUP`,
исправлен на правду (`9364e2c87`). Сама реализация — Job Object с
`JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, назначение процесса при запуске
через `CREATE_SUSPENDED` — **осознанно не сделана**: ни эта машина, ни
локальный прогон её не исполнят, первым исполнением стал бы Windows-
раннер CI, а ошибка в порядке назначения задания проявляется только в
рантайме. Делать отдельной работой, с реальной Windows-машиной.

### B2 [закрыт] Гонка `discard` в пуле соединений `db`

`internal/db/connect.go`: ожидающий в `entry.wait()` получает
`(conn, nil)`, потому что `wait()` возвращает только `entry.err` и
никогда не сверяется с `discard`. Открывающий следом видит `discard` и
закрывает тот же `*sql.DB` — у ожидающего на руках остаётся закрытая
ручка при `err == nil`, а его последующий `Release` сообщает о
несуществующем злоупотреблении.

Срабатывает только после непарного `Release` — то есть это второй баг
поверх злоупотребления, а не достижимый из корректной пары
`Connect`/`Release`. Но исход хуже уже принятого техдолга: там закрытие
детерминировано внутри `Release`, здесь законному ожидающему сообщают
об успехе за мгновение до инвалидации.

Закрыт в `d48f4adab`: при `discard && err == nil` открывающий
перезаписывает `entry.err`, так что `wait()` сообщает отказ каждому
ожидающему, а не только своей горутине.

---

# Аудит 5 (2026-09-03, `main`, 756e6ae40)

`fsext`, `git`, `cmd` — три пакета, не покрытые предыдущими. Две
находки, обе подтверждены и закрыты.

**Неполнота листинга доходит до модели** (`c9657d967`). Все три обхода
каталога возвращали `nil` на ошибку, а комментарий винил права доступа —
хотя `fastwalk` отдаёт эту ошибку от неудачного `ReadDir` каталога, то
есть сюда же попадают удаление на лету, `EMFILE` и сбой сетевого
монтирования. Флаг `truncated` брался только из усечения по лимиту.
Инструменты `ls` и `glob` получили **отдельный** сигнал `Incomplete`:
`Truncated` значит «сработал лимит, курсором дочитаете», `Incomplete` —
«часть дерева не прочиталась, курсор не поможет». Сводить их в один флаг
нельзя — это предложило бы модели листать там, где листать нечего.

**`git.IsRepo` не отличал «нет» от «не смог узнать»** — то же, что
`BranchExists`, но с усугублением: ошибка не терялась, а переименовывалась
в `ErrNotARepo`, который потребитель в `session_changes.go` **специально**
не логирует. Панель изменений показывала файлы согласованными с git, хотя
этого никто не выяснял.

## Правило, выведенное из трёх повторов

`BranchExists`, `IsRepo`, обходы каталога — один и тот же дефект трижды:
**функция, возвращающая `bool` или пустой результат там, где операция
может не выполниться, лжёт вызывающему.** Проверять при code review любую
обёртку над подпроцессом или обходом ФС: есть ли у неё способ сказать «не
знаю», и не превращает ли вызывающий это «не знаю» в «нет».

---

# Аудит 6 (2026-09-03, `main`, c84cc15c6)

`csync`+`pubsub`, `shellconfig`+`commands`+`richpaste`,
`ui/chatlist`+`ui/list`+`ui/diffview`. Три находки, все закрыты.

По `csync` и `pubsub` находок нет — итераторы снимают копию под локом и
не держат его на теле цикла, аккаунтинг потерь и путь
`PublishMustDeliver` соответствуют документации. Закрыта одна латентная
дыра: `GetOrSet` и `CompareAndDelete` мутируют карту, но обходили
заморозку (`0bdab1c84`).

По `shellconfig` находок нет: гейт `ConfigBuilder` полон во всех
встроенных командах, а глобальные ключи вырезаются из проектного слоя в
shell-пути так же, как в JSON (`load.go:239-243` против `254-258`).

## Открыто после шестого аудита

### C1 [закрыт] У `fetch` и `download` нет защиты от SSRF

Обнаружено при починке `richpaste`. Ни `internal/agent/tools/fetch.go`,
ни `download.go` не проверяют адрес: `newHTTPClient`
(`tools/tools.go:317`) клонирует `http.DefaultTransport` и настраивает
только пул и таймауты — ни `CheckRedirect`, ни проверки на уровне
дозвона. `internal/proxyhttp` занимается маршрутизацией через прокси,
`internal/dns` — резолвингом на Termux; ни один SSRF не адресует.

Смягчающее обстоятельство настоящее, но узкое: оба инструмента проходят
через `permission.Requester`, то есть пользователь видит конкретный URL и
подтверждает его до запроса. Поэтому у вставки из буфера (где шлюза не
было вовсе) приоритет был выше — но здесь остаётся: подтверждённый
пользователем адрес может **редиректить** во внутреннюю сеть, а редиректы
не проверяются.

Чинить тем же приёмом, что применён в `richpaste/resolve.go`: страж на
`DialContext`, резолвящий имя и подключающийся к разрезолвленному IP, плюс
`CheckRedirect` на каждый хоп.

### C2 [закрыт] Высота элемента может измениться под прокруткой

Найдено при починке `RemoveItem`. Инвариант теперь записан: `offsetLine`
измеряется против конкретного элемента в слоте. Но `Invalidate` и
обновления при стриминге меняют высоту элемента, не трогая `offsetLine`,
— если элемент под прокруткой ужмётся, получится тот же пустой вывод, что
и в закрытом баге, только через другой триггер.

Закрыт: `renderItemEntry` подрезает `offsetLine` при сжатии последнего
элемента, а `currentOffsetLine()` стал обязательным доступом для всех
читателей, кроме тех, кто это поле пишет.

## Ловушка, дважды за сессию выглядевшая «невоспроизводимым флаком»

`internal/agent`'s `captureLogs`/`captureJSONLogs` подменяют
**процессно-глобальный** `slog.SetDefault`. Мьютекс `logCaptureMu`
(`common_test.go:186`) сериализует захватывающие тесты друг относительно
друга — но не может ничего сделать с параллельным тестом, который логов
не захватывает: тот пишет в тот же глобальный логгер, то есть в чужой
буфер.

Следствие: тест, ищущий свою строку **только по тексту сообщения**,
поймает чужую. Так дважды падал `TestInstrumentedModel_Success` (я списал
на невоспроизводимость) и один раз на Windows —
`TestTrimToBudgetLogsCorrelationIDs`.

Правило для любого теста, читающего захваченные логи: **отбирать строку
по своему идентификатору** (сессии, прогона), а не по тексту, и держать
«строка не появилась» отличимым от «появилась, но не наша».

**Пройдено, и оценка «49 мест» оказалась неверной.** Все 49 вызовов
`captureLogs`/`captureJSONLogs` разобраны по одному: в риске был **один**
(`aws_sso_refresh_test.go` проверял четыре поля четырьмя независимыми
`Contains` по всему буферу, так что они могли прийти из разных строк).
Остальные уже привязаны — 30 через `findProviderLogLine`/
`allProviderLogLines`, которые сверяют и `msg`, и `session_id`, 11 через
`repairLine`-семейство, прочие фильтруют вручную.

Отдельно: проверки вида `NotContains` по уникальному маркеру, который
тест сам же и сгенерировал (`continuation_test.go`), в риске не были
никогда — чужая строка способна дать там только ложное падение, но не
ложный успех.

Правило записано в doc-комментарий `captureLogs`, а `syncLogBuffer.Lines`
делает привязку к идентификатору дешёвой для будущих тестов.

## Как читать этот документ дальше

Девять находок из семнадцати проверку не пережили, и все девять были
выведены из метрик: размер файла, счётчики импортов, имена файлов,
отношение комментариев к коду, срабатывания линтера. Всё, что найдено
чтением кода, устояло.

Поэтому: **находку, полученную из метрики, проверяют чтением
потребителей до того, как заводить под неё работу.** Дешёвая проверка
экономит откат, а сама проверка обычно даёт знание ценнее исходной
находки — так выяснилось, что тридцать файлов UI импортируют `proto`
под псевдонимом, что запись креденшела и есть запись конфига, и что в
контракте, который линкует UI, живёт подпроцесс.

# Аудит 8 (2026-09-03, `main`, b04a0e592)

Три области, выбранные по признаку «данные приходят снаружи и им верят»:
арифметика денег (`stats`, `stats/gather`, `modelcache`), разбор чужого
формата (`importer`, `discover`, `frontmatter`) и обработка чужого
процесса и управляющих последовательностей терминала (`herdr`,
`ansiext`, `format`, `question`).

Третья область находок не дала. Сокетный протокол `herdr` оказался
односторонним — ответ вычитывается в `io.Discard`, разбора входящих байт
в пакете нет вовсе, так что «битое сообщение от соседа» не имеет пути к
коду. `ansiext.Escape` покрывает все C0 и DEL, включая ESC. Записываю это
здесь, потому что «проверено и чисто» — такой же результат аудита, как
находка, и второй раз читать то же самое незачем.

## D1 [закрыт] Импорт скилла копирует содержимое по симлинку

`copyDir` (`internal/importer/importer.go`) обходит дерево через
`filepath.WalkDir`, который по симлинкам **не** идёт, — и читает файл
через `os.ReadFile`, который идёт. Симлинк приезжает в колбэк как
обычный файл, и содержимое его цели уезжает в `.sennit/skills/<name>/`.

Каталог-источник недоверенный по определению: `sennit import --skills`
существует ровно для того, чтобы принимать скачанные и скопированные
откуда-то каталоги.

## D2 [закрыт] Два из четырёх обогатителей `discover` не проверяют знак

`llamacpp.go` и `lmstudio.go` проверяют пришедший от сервера размер на
`> 0`. `litellm.go` и `omlx.go` — нет, только на `!= nil`. Отрицательный
`ContextWindow` доходит до `Model.CatalogCfg` и участвует в решении об
автосуммаризации: `stopOnContextWindow` и `summarizeBuffer` особым
случаем считают только `cw == 0`.

## D3 [закрыт] Стоимость делегирований не попадала в итоги

Комментарий `ComputeTotals` утверждал, что расход дочерней сессии уже
свёрнут в родительскую. Механизм для этого — `Service.AddCost` — не имел
ни одного вызова в продакшене: единственный вызывающий удалён коммитом
`28db54638` с обоснованием «finalized atomically by the store», а такой
финализации не существует.

Свёртку **не восстанавливали**, и это главное решение по находке.
`ComputeModels` и `ComputeAgents` суммируют все сессии подряд; вернув
свёртку, мы получили бы стоимость ребёнка в двух строках сразу и
удвоение в этих двух разрезах. То есть до удаления схема тоже была
несогласованной — ошибка просто сидела в другом месте. Правильная
инвариантa: **одна запись расхода на сессию, агрегация чинится в
агрегаторе.**

Мёртвый механизм снят вместе с находкой: `Service.AddCost` и запрос
`AddSessionCost` удалены. Комментарии, называвшие `AddCost` конкурирующим
писателем строки сессии, переписаны на настоящего — генерацию заголовка,
которая уходит в отсоединённую горутину посреди хода и пишет
`cost = cost + ?` в ту же сессию.

Деньги и токены теперь суммируются по всему дереву, а `Sessions` и
`TimeSeconds` остались верхнеуровневыми — и это не непоследовательность:
делегирование не является сессией, которую человек запустил, а его время
течёт внутри времени родителя, так что сложение насчитало бы больше, чем
реально прошло.

## D4 [закрыт] Сайдбар не показывает расход делегирований

`internal/ui/model/sidebar.go` берёт `m.sess.current.Cost` — собственную
строку сессии. При выбранной инвариантe «одна запись расхода на сессию»
расход делегирований туда не попадает.

Для задач, запущенных через `task`, он не попадал и до удаления свёртки:
та срабатывала только при пустом `ChildSessionID`. Так что это не
регрессия, а более старый и более широкий пробел, и чинится он не
свёрткой, а живой суммой по дереву сессии.

---

# Состояние на 2026-09-03, `546ece221`

Устарело после аудита 11: открытых дефектов 25, см. фазу 1 ниже.
Записи этого блока про B1, 5.4, 2.2 и `NewStore` остаются в силе. Всё, что ниже, — не
«руки не дошли», а решения с причиной; каждое стоит пересматривать
только при наступлении названного условия.

**B1, Windows не убивает дерево процессов.** Ждёт Windows-раннера.
Написать код под платформу, на которой его негде выполнить, и пометить
пункт закрытым — хуже, чем оставить пробел задокументированным.

**5.3, сетевой вызов в контракте `workspace` — закрыт** (аудит 11
нашёл, что документ держал открытым уже решённое: `go list -deps
./internal/workspace | grep discover` пуст).

**5.4, рендер Docker MCP.** Отдельный пакет оправдан при появлении
второго такого вендора, не раньше.

**2.2, остаток `delegationFinalizer`.** Резать по причине, а не ради
счёта полей.

**Узкие интерфейсы в `ui/model` и `ui/dialog`.** Читаемость, не
связность: проверено — линковка не изменится.

**`config.NewStore` без заморозки.** Цена замерена, выгоды нет; см.
запись выше.

---

# Аудит 9 (2026-09-03, `main`, 546ece221)

Три области, не читанные ни разу, выбранные по признаку «охраняет
границу»: `oauth` (чужие токены), `shell`/`shellconfig` (исполнение
команд), `lock`/`workspacelock`/`filetracker` (инварианты, на которые
опираются правки файлов).

Отдельно прогнана механическая проверка по всем 352 методам интерфейсов
`internal`: у скольких нет вызывающих вне тестов.

## E1 [закрыт] Обход проверки «прочитано перед правкой»

Пустая строка значила одновременно «файл прочитан целиком» (в хранилище,
включая строки, записанные до появления диапазонов) и «строки ещё нет»
(в `updateFileReadIn`). Воспроизведено: после `RecordPartialRead(1,500)`
на новом файле покрытие становится `Full`.

Следствие достигалось обычным путём: `read_core.go` уходит в частичную
ветку при усечении вывода по бюджету или ненулевом смещении, так что
чтение большого файла помечало прочитанным весь файл, и `edit` на строке
2800 проходил проверку.

Второй путь примечателен: в `lsp_replace_symbol.go` стоит комментарий,
объясняющий, почему там намеренно не ставится «прочитан целиком» — иначе
позднейшая правка изменит любую строку файла, который сессия не
открывала. Через `RecordEdit` на новой строке происходило ровно это.

## E2 [закрыт] Восстановленный токен MCP OAuth без refresh — тупик

`hasRefreshableToken` не проверял refresh-токен, хотя его комментарий
обещал «enough state to be used and refreshed». Плюс `SetExpiresAt` при
отсутствии срока пишет `expires_at: 0`, что при восстановлении даёт
1970 год — не `IsZero()`, то есть «давно истёк».

Сервер, выдавший bearer без refresh, после перезапуска не подключался
никогда: `oauth2` отказывает **до** отправки запроса, значит 401 не
приходит, значит браузерный поток недостижим; ошибка не совпадала ни с
одним образцом в классификаторе и уходила в `StateError`, а не
`StateNeedsAuth`, поэтому UI не предлагал войти заново. Выход был один —
руками вычистить токен из конфига.

## E3 [закрыт] Ненулевой код возврата последней строки `sennitrc` не давал стартовать

Воспроизведено: `false` в конце файла даёт `exit status 1`, и весь уже
собранный конфиг отбрасывается. Идиома `command -v gopls >/dev/null &&
lsp add …` на машине без `gopls` роняет запуск. Ни один shell так себя
не ведёт.

## E4 [закрыт] `model remove` после `provider remove` портит tombstone

`modelRemove` писал `"models"` рядом с `__sennit_tombstone`, а
`ParseTombstone` отвергает запись с двумя полями — конфиг переставал
загружаться целиком, с сообщением про tombstone, не связанным с тем, что
написал пользователь. У `modelAdd` от этой же опасности стоит явная
защита с комментарием.

## E5 [закрыт] Окно слепоты до секунды в проверке свежести файла

`read_at` хранится с точностью до секунды (`strftime('%s','now')`), и
сравнение подрезает mtime до секунды. Внешняя запись в ту же секунду,
что и чтение, невидима: `write` перезапишет правку пользователя молча.

Дешёвого исправления нет. Сравнивать «больше либо равно» нельзя: обычная
последовательность «прочитал и сразу правлю» укладывается в ту же
секунду и начнёт отказывать. Нужна субсекундная точность колонки, то
есть миграция.

## E6 [закрыт] `HERDR_*` не вычищаются на путях `RunAndCapture*`

`NewShell` чистит переменные, hook-раннер чистит, а `RunAndCapture` и
`RunAndCaptureStream` — нет. Вложенный `sennit` из bang-режима или
`sennitrc` подхватывает сокет и pane-id родительской панели, перехватывает
authority и на выходе снимает её — статус родителя из панели исчезает.
Инвариант закрыт на двух поверхностях запуска из трёх.

## E7 [закрыт] Отмена команды неотличима от «команда вернула 1»

`RunAndCaptureStream` всегда возвращает `error == nil`, сворачивая всё в
`ExitCode`, а `ExitCode` для не-`ExitStatus` отдаёт 1. Поэтому ветка
`errors.Is(err, context.Canceled)` в `ui/model/shell.go` недостижима, а
поле `shellResultMsg.Canceled` не читается нигде. Пользователь, нажавший
Esc, видит «exit 1», и это уходит в историю сессии как обычный провал.
Текст ошибки разбора (`could not parse command`) теряется полностью.

## E8 [закрыт] Config-builtins перехватывают одноимённые программы из PATH

`builtinHandler` стоит первым в цепочке, раньше блок-листа и
`scriptDispatch`. Без `ConfigBuilder` на контексте обработчик — no-op,
возвращающий `nil`, то есть статус 0. Команда `mcp dev server.py`
(реальный CLI из Python-пакета `mcp`) не выполняется, ничего не печатает
и рапортует успех. Так же для `model`, `option`, `hook`, `provider`,
`lsp`, `permissions`.

## E9 [закрыт] Файл без бита исполнения выполняется как shell-скрипт

`scriptDispatchHandler` решает по содержимому, права не смотрит вовсе.
Настоящий shell дал бы `permission denied` и 126. Снятый `+x` —
обычный способ обезвредить скрипт, и здесь он не работает; любой
текстовый файл данных будет построчно исполнен. Обратная сторона: файл
`0711` (исполняемый, но нечитаемый) здесь падает, хотя shell его
запустил бы.

## E10 [закрыт] Шебанг длиннее 128 байт молча обрезается

`probeFile` читает 128 байт, а `parseShebang` не проверяет, нашёлся ли
перевод строки внутри окна. Длинный `#!/usr/bin/env -S …` или
интерпретатор в глубоко вложенном пути дают argv из огрызка — последний
токен режется посередине — без единой диагностики.

## Проверено и чисто

`internal/lock` и `internal/workspacelock`: ни один путь не рапортует
успех, не удерживая блокировку; повторное освобождение безвредно на
обеих платформах; правила устаревания нет и не нужно — файл не
удаляется, а flock снимается ядром при смерти процесса. Ключи путей в
`filetracker` сводятся через `fsext.Canonical`, так что относительные
пути, `..` и симлинки складываются в один ключ. По POSIX-пути жизненного
цикла процессов в `internal/shell` (`Setsid`, `kill(-pid)` с эскалацией,
гонка с `reaped`) претензий нет. PKCE и state сверяются до реакции на
`error=`; ни токен, ни секрет, ни код авторизации не попадают в лог.

## Что дала механическая проверка

Из 352 методов интерфейсов `internal` без вызывающих вне тестов
оказалось 12. Из них `PrepareContext` — часть стандартного `DBTX` от
sqlc, восемь — мёртвые SQL-запросы, три — осиротевшая ветка UI.

Ценным оказался не список, а **вопрос**. `check_deadcode.sh` намеренно
спрашивает «недостижимо ли и из тестов тоже» — иначе выдаёт 85 живых
символов. На осиротевшую ветку панели он отвечает «достижимо»: её
покрывают шестнадцать тестов. Вопрос «есть ли вызывающий вне тестов»
отвечает «нет».

## Чем обернулись починки девятого аудита

Три правки пришлось вернуть авторам, и все три — не про логику, а про
то, что правка задевала сверх задания.

**E5.** Подрезка `modTime` была снята целиком, а не переведена на новое
разрешение. Это вернуло бы ту же ошибку зеркально: наносекундное время
изменения против времени чтения, округлённого вниз до миллисекунды,
объявляет устаревшим файл, который агент сам только что записал.
Подрезка — это симметрия, а не костыль, и следует за разрешением
колонки.

**E6.** Вычистка переменных была расширена правильно — нашлись git с его
репозиторными хуками, stdio-сервер MCP и подстановка `$(cmd)` при
разрешении конфига. Но `WithoutHerdrEnv` осталась в `internal/shell`, и
базовый `internal/env` начал импортировать машинерию с интерпретатором.
Функция чистая, фильтрация `[]string`, — переехала в `env`.

**E9.** Файл, исполняемый через дополнительную группу, читался как
неисполняемый, и это было названо безопасным отказом. Отказ запустить
разрешённое — не безопасный: `0750` с групповым владельцем на общей
машине обычное дело. `os.Getgroups()` закрывает случай.

Отдельно: unscoped `git stash` одного из агентов смёл работу соседей.
Потерь не было — сверил заначку с деревом пофайлово, все расхождения
оказались движением вперёд, — но при нескольких агентах в одном дереве
это ход, которого делать нельзя.

---

# Аудит 10 (2026-09-03, `main`, fe6f3b462)

Области: `permission` + `hooks` (вся поверхность решения «что позволено»),
`fsext` и помощники ограничения путей, `history` + `skills`.

Само решение «внутри или снаружи рабочего каталога» сломать не удалось:
`resolveWithinWorkdir` разрешает обе стороны до сравнения, `HasPrefix`
считает через `filepath.Rel`, любая ошибка разрешения даёт «снаружи».
Пять из шести находок — про то, что происходит **после** решения.

## F1 [закрыт] Безголовый запуск с делегированием зависал навсегда

`sennit run` выдаёт автоодобрение одной сессии, а делегирование живёт в
дочерней. Запрос уходил в брокер без подписчиков, на контексте
диспетчера задач, а сторож, который завершает застрявшее делегирование,
намеренно отступает как раз при ожидании ответа. Ни вывода, ни
диагностики — до убийства процесса.

Чинилось не безусловным одобрением дочерних сессий: такое уже стояло в
коде и было удалено, потому что позволяло делегату чтения читать что
угодно на диске. Флаг распространяется по ребру делегирования и только
когда он есть у родителя.

## F2 [закрыт] Правка симлинка уничтожала ссылку, не трогая файл

Чтение шло по ссылке, `rename` заменял саму ссылку. Разницу показывали
для цели, сверяли с целью, ломали ссылку, рапортовали успех, а в историю
писали путь, содержимое которого не менялось.

## F3 [закрыт] Старшинство скиллов решал алфавит, а не конфигурация

`Deduplicate` оставляет последнее вхождение, а сортировка перед ним
стирала порядок настроенных каталогов. Проект в `~/Projects` побеждал
глобальный скилл, проект в `/data` или `C:\` — проигрывал.

## F4 [закрыт] Диалог разрешений показывает неразрешённый путь

Решение принимается по разрешённому пути, а в окне печатается исходный.
При `proj/cache -> ~/.ssh` пользователь видит путь внутри своего проекта
и подпись «вне рабочего каталога» — единственный факт в окне
противоречит причине, по которой оно открылось. Тот же путь служит
ключом постоянного разрешения, так что «разрешать всегда» записывается
на имя ссылки и переезжает вместе с ней.

## F5 [закрыт] `ls` на симлинке каталога возвращает пустоту

`filepath.Walk` делает `lstat` корня и не спускается; `os.Stat` в
охранной проверке идёт по ссылке и молчит. `glob` на том же пути через
`fastwalk` выдаёт всё содержимое. Модель сообщает «каталог пуст» на
раскладке `deploy/current -> releases/...`.

## F6 [закрыт] Мёртвый `FormatInvocation`

`customCommandItem` при непустом `Skill` всегда строит
`ActionAttachSkill`, поэтому ветка `if msg.Skill != nil` в
`dialog_actions.go` недостижима, а `FormatInvocation` не вызывается
ниоткуда. Комментарий у неё описывает поведение, которого нет, — третий
такой случай за сессию.

Отдельный вопрос, который стоит задать отдельно: `attachSkill`
прикладывает сырое содержимое `SKILL.md`, а документация обещает, что
такой скилл «ведёт себя как команда». Это не дефект с доказанным
исходом, но расхождение между обещанием и поведением.

## F7 [закрыт] `thread_create` поднимает свой `permission.Service`

Найдено при починке F1. Тред создаёт изолированное приложение с
собственной службой разрешений; запрос изнутри ретранслируется в поток
событий родителя, но никто на него не отвечает автоматически. Если
`thread_create` достижим из безголового запуска — это структурно другое
зависание, F1 его не закрывает.

## Проверено и чисто

Хуки открываются наружу при любом отказе — старт не удался, таймаут,
битый вывод, — и это ровно то, что обещает их документация, включая
таблицу кодов возврата. Записано, чтобы следующий аудит не выводил это
заново. Выделение версий файлов защищено транзакцией, `_txlock=immediate`
и единственным соединением; коллизия или пропуск номера невозможны.
Восстановления версии в коде нет вовсе, хотя doc-комментарий пакета его
обещает. Экранирование в `ToPromptXML` и `FormatInvocation` делает
попытку выдать себя за системные инструкции из чужого `SKILL.md`
безвредной.

## Что осталось открытым вопросом, а не задачей

`attachSkill` прикладывает сырое содержимое `SKILL.md` вложением, а
документация обещает, что скилл с `user-invocable: true` и
`disable-model-invocation: true` «ведёт себя как команда». Приложенный
файл модель прочитает, так что доказанного неверного исхода нет — но
обещание и поведение расходятся. Выбор между «поменять поведение» и
«поменять обещание» продуктовый, не технический.

## Четыре мёртвых механизма с живым описанием

`AddCost`, `CreateTitleSession`, осиротевшая ветка живой активности
панели и `FormatInvocation`. Каждый выглядел работающим и документировал
поведение, которого в системе нет; первый из них прямо ввёл в
заблуждение `ComputeTotals` и стоил пользователю всех денег
делегирований.

Ловятся они одним вопросом — **есть ли вызывающий вне тестов** — и не
ловятся ни линтером, ни `check_deadcode.sh`: скрипт намеренно спрашивает
про достижимость включая тесты, иначе выдаёт 85 живых символов. Проверка
механическая: собрать методы интерфейсов и грепнуть `.Метод(` по дереву
без `_test.go`. Важно брать точную форму вызова: подстрочный поиск даёт
ложные срабатывания там, где имена образуют семейство (`GetFile` против
`GetFileRead`, `GetFileByPathAndSession`, `GetFileDiagnostics`).

---

# Аудит 11 (2026-09-04, `main`, 3ca268637)

Семь областей, которых не касался ни один из десяти предыдущих аудитов:
жизненный цикл тредов (`thread`, `threadspawn`, `ui/threads`), MCP-клиент
и учётные данные (`tools/mcp`, `providerload`, `providers/accounts`,
`oauth/*`), хранилища (`message/store`, `session/store`, `gc`, `db`),
ядро хода (`agent` без `tools/`), мутирующие и LSP-инструменты, листовые
пакеты (`log`, `home`, `commands`, `proxyhttp`, `migrate` и другие),
UI-диалоги и вложения. Каждая область читалась по потребителям с тем же
стандартом приёмки: сценарий, путь с `file:line`, последствие. Сверх того
— граф импортов `internal/ui` (чист: ни `db`, ни `agent`, ни `app`), 515
методов интерфейсов на отсутствие вызывающих вне тестов (один), и
детектор клонов по продакшн-коду.

Итог: 25 дефектов, 4 нарушения границ, 5 пунктов DRY/мёртвого кода.
Ни одна находка не выведена из метрики; метрики (детектор клонов,
подсчёт вызывающих) использованы только как указатель, куда читать, и
каждая подтверждена чтением потребителей или репродукцией.

**Состояние: закрыто всё, кроме B3 и B4.** Хеш коммита стоит у каждого
пункта. Ни одна находка при реализации не отвалилась — все 32 оказались
настоящими. B3 (продолжение делегации выполняет агент-кодер) требует
передела, а не правки, и помечен «не делать наспех»; B4 — продуктовое
решение, подключать ротацию к делегациям или задокументировать отказ.

## Фаза 1. Баги

По убыванию тяжести. Первые три — потеря данных или зависание, чинить
до всего остального.

### G1 [закрыт, `59863fd6f`] Безголовое разрешение теряется при респауне треда

`df020bef1` (F1/F7) пробрасывает грант только в `Manager.Create`
(`thread/manager.go:356-362`). Грант живёт в `permission.Service`
App'а треда, а этот App уничтожается при каждом освобождении рантайма:
`finalizeRunComplete` → `releaseRuntime` (`lifecycle.go:921`) →
`LocalSpawner.Release` → `a.Shutdown()` (`threadspawn/spawner.go:147`).

Сценарий на штатных инструментах `sennit run`: `thread_create` → ран
завершился с `conflict` → документированный путь «resolve via Send and
Merge again» (`merge.go:36-38`) → `agent_send` → `lifecycle.send` при
`rt == nil` делает `Spawn` (`lifecycle.go:675`) → новый `Service` без
гранта. Ни `Manager.send` (`manager.go:760-799`), ни `Activate`
(`manager.go:619`) не зовут `AutoApproveSession`. Первый `edit` блокируется
на канале ответа; сторож обходит только `KindTask` (`tasks.go:352-364`,
`watchdog.go:83`) и по замыслу не трогает ожидание разрешения. Процесс
виснет без вывода.

Правка: выдавать грант на общем пути установки рантайма.
`registerThreadParent` уже вызывается на всех трёх путях, но в `send`
идёт после `startRun` — ставить до диспетча. Тест: копия
`TestManager_CreateInheritsAutoApprovalFromApprovedParent` с завершением
рана и последующим `Send`.

### G2 [закрыт, `23a17963d`] `lsp_replace_symbol` режет файл по устаревшим границам

`lsp_replace_symbol.go:88-99`: `DocumentSymbols` → сразу
`applyFileMutation`, без `checkFileFreshness` и `requireReadCoverage`,
которые есть у `edit.go:301-320`, `write.go:66-89`, `multiedit.go:183`.
Диапазоны приходят из overlay LSP: `OpenFileOnDemand`
(`lsp/client.go:284-295`) возвращается сразу, если файл открыт, повторный
`read` `didChange` не шлёт, `bash` не зовёт `notifyLSPs` вовсе (вызовы
только в `filemutation.go:259`, `lsp_rename.go:162`, `diagnostics.go:37`).
`readFileSnapshot` (`filemutation.go:152`) читает диск. После `gofmt -w`
или правки в редакторе `lines[start:end+1]` вырезает не ту функцию; при
`delete` теряется чужой код; под yolo диалог с неверным diff никто не
видит. `TestReplaceSymbolThroughManagerRecordsOnlyTheEditedSpan`
закрепляет работу без единого `read`, а `lsp_replace_symbol.md` говорит
«Prefer this over edit».

Одной проверки свежести мало: после `gofmt` `edit` откажет, модель
перечитает файл, а LSP останется на старой версии. Правка: перед
`DocumentSymbols` — `OpenFileOnDemand` + `NotifyChange`
(`filesync.go:105-118` перечитывает диск целиком), плюс тот же switch по
`checkFileFreshness`, что в `write.go`.

### G3 [закрыт, `f0676ae0f`] Переименование сессии из TUI пишет всю строку из снимка

`ui/dialog/sessions.go:242,258` берёт сессию из снимка `ListSessions`
(`dialogs.go:433`, pubsub-обновлений диалог не получает) и зовёт
`SaveSession` → `sessionstore.Save` (`service.go:229`) → `UpdateSession`
(`sessions.sql:64`), который пишет `title, prompt_tokens,
completion_tokens, summary_message_id, cost, todos` целиком. Пока диалог
открыт, ход завершается (`cost = cost + ?`), todo пишет список, или
проходит суммаризация (`usage.go:424`). Подтверждение стирает стоимость
(навсегда — других писателей `cost` нет), откатывает todo и возвращает
`summary_message_id` к старому: `trimToSummary` (`usage.go:467`) отдаёт
несжатую историю, следующий ход суммаризирует заново за деньги.

Хранилище само описывает этот класс у `SetTodos` (`service.go:60-63`).
Узкий `Rename` (`service.go:338`) есть, но им пользуется только CLI
(`cmd/session.go:279`). Правка: `Workspace.RenameSession` → `Rename`;
`SaveSession` после этого без вызывающих вне тестов — удалить.

### G4 [закрыт, `168d80daf`] Отмена во время авто-суммаризации публикуется как ошибка

`run_turn.go:519-527`: ветка «ничего не в очереди» кладёт
`summarizeFailed.Error()` в `RunComplete` без `Cancelled`, тогда как
соседняя `:561-573` выставляет его по `errors.Is(err, context.Canceled) ||
ctx.Err() != nil`. `thread/lifecycle.go:947-949` по `Error != ""` даёт
`StatusFailed`, тред финализируется как «failed: context canceled» и так
сообщается родителю; в TUI — «Task finished» на Escape, без
`FinishReasonCanceled` в транскрипте. Правка: то же правило в первой
ветке; `AgentFinished` при отменённой суммаризации не публиковать.

### G5 [закрыт, `864ab6b16`] Браузерный вход в MCP ограничен таймаутом запуска сервера

`tools/mcp/connection.go:121-122`: `mcpCtx` получает `AfterFunc(timeout,
cancel)`; под ним `client.Connect` → 401 → `transport.go:191`
`Authorize(req.Context(), …)` → `oauth/mcp/handler.go` `await` ждёт
`ctx.Done()`. Вход через SSO с 2FA дольше 30 с (или пользовательского
`timeout 10`) обрывается; `context deadline exceeded` не `Canceled` и не
`isOAuthInitErr` → `setAuthTerminal` (`authcoordinator.go:41-45`) →
`StateError` с сырым текстом вместо «нужен вход». `docs/extending/mcp.md:73`
обещает, что `timeout` ограничивает только старт сервера;
`transport.go:254-257` — «generous default» для браузера. Правка:
`Authorize` под `context.WithoutCancel` + собственный интерактивный лимит,
привязанный к `flow.cancel`.

### G6 [закрыт, `23a17963d`] grep-фоллбэк на симлинке-каталоге возвращает пусто

`grep.go:430` `filepath.Walk(rootPath)` делает `lstat` корня. F5 закрыл
это для `ls` (`fsext/ls.go:262-271`), `glob` через `fastwalk` stat-ит
корень, `rg` получает путь явно — pure-Go `grep`, регистрируемый без `rg`
(`tool_registry.go:112-114`), единственный отстал. Воспроизведено:
`current -> releases/v1`, `searchFilesWithRegex(link)` = 0 совпадений,
по цели = 1. Там же `grep.go:435` `return nil // Skip errors` — класс
аудита 5: `ls`/`glob` получили `Incomplete`, `grep` молчит. Правка: тот
же резолв корня, что в `VisitDirectory`, и флаг неполноты в метаданных.

### G7 [закрыт, `0ccc91741`] Два запуска без файлового лога

Один инвариант, две поверхности (класс 7).

`cmd/run.go:95`: `--verbose` делает `slog.SetDefault(charm log)` **после**
`sennitlog.Setup` — файловый handler выброшен, `sennit logs` для такого
запуска пуст. При этом `log.Setup(path, debug, ws ...io.Writer)`
(`log/log.go:29`) умеет «текст на терминал, JSON в файл» — и не имеет ни
одного вызывающего с writers (`root.go:259` без них). Мёртвый механизм с
живым описанием, переизобретённый снаружи с потерей.

`doctor`, `stat`, `models`, `logs`, `session`, `gc`, `import` идут через
`initConfig` (`cmdutil.go:39`) без `Setup`/`Replay`: `EarlyHandler`
копит записи в память до выхода. `migrate.go:88` «Config key deprecated»
и `DropIncompatibleRecentModels` — предупреждения, ради которых существует
`doctor`, — не видны нигде.

Правка: `Setup(path, debug, os.Stderr)` при `verbose`, `run.go:95`
удалить; `Setup`+`Replay` вынести в helper и звать из `initConfig`.
Проверить выбор файла в `logs.go` — иначе «самый новый лог» окажется от
`sennit stat`.

### G8 [закрыт, `0ccc91741`] `@`-вставка файла без лимита размера и типа

`ui/model/editor_input.go:249-262` `insertFileCompletion`: `os.ReadFile`
без `MaxAttachmentSize`; остальные пять путей (`:357,477,531,579,630`,
`dialog/actions.go:347`) проверяют 5 МБ. Список — `fsext.ListDirectory`,
там `.sqlite`, `.pack`, `.mp4`. Блоб уходит в `BinaryContent`
(`agent/session_call.go:235`), в SQLite и в каждый следующий запрос как
`application/octet-stream` (`message_convert.go:57-66`). Правка: `Stat` +
лимит + для не-текста и не-картинки — только путь в тексте.

### G9 [закрыт, `0ccc91741`] Превью в file picker декодирует без лимита пикселей

`ui/dialog/filepicker.go:208-219`: `MaxPreviewSize` (2 МБ) по байтам,
затем `image.Decode`. PNG 20000×20000 однотонной заливки — 1.5 МБ на
диске и 1.6 ГБ RGBA в памяти; срабатывает на подсветке, не на выборе
(`:165-167`). `DecodeConfig` в UI не используется. Правка: `DecodeConfig`
→ отказ выше порога → `Seek(0)` → `Decode`.

### G10 [закрыт, `59863fd6f`] `Remove` рушит живой воркспейс раньше проверки, которая может отказать

`thread/manager.go:888-901`: `c.removed = true`, `runtime = nil`,
`releaseRuntime` → `App.Shutdown()` — и только на `:920-926`
`deleteBranch && !force → IsAncestor`, который для idle-треда отказывает
«not merged; use force». Строка остаётся `idle` (контракт «workspace is
live», `types.go:16-22`), а App, к которому привязан экран человека
(`ui/model/root.go:611-618`), уже мёртв: экран перестаёт обновляться,
следующий ввод спавнит новый App, событий которого экран не видит.
Класс 4. Правка: поднять проверку выше `:888` — она читает только
`repoRoot`.

### G11 [закрыт, `59863fd6f`] `Send` возобновляет тред без ворктри

`Activate` проверяет `os.Stat(st.WorktreePath)` (`manager.go:615`);
`Manager.send` (`:777`) и `lifecycle.send` (`:675`) — нет. После
`failCreate` + `unwinder` (`:311-318`) строка `failed` видна в
`agent_list`; `agent_send` → `Bootstrap` намеренно принимает
несуществующий путь (`bootstrap.go:258-262`) и делает `MkdirAll`
(`:281`) → `running` в пустом каталоге вне git → `merge_blocked` с
git-ошибкой; `Remove` потом не проходит. Правка: тот же `Stat` в
`Manager.send` (не в `lifecycle.send` — он общий с задачами, у которых
`spawnPath == ""`).

### G12 [закрыт, `864ab6b16`] MCP `EmbeddedResource`/`ResourceLink` уходят модели как дамп указателя

`tools/mcp/tools.go:87-88` `default: fmt.Sprintf("%v", v)` для
`*mcp.EmbeddedResource` (`Resource *ResourceContents`) печатает
`&{map[] <nil> 0xc000…}`. Сервер отвечает на `get_file`
embedded-ресурсом — модель видит адрес памяти при `IsError=false`.
Правка: ветки на оба типа (`Resource.Text`/`Blob` по MIME; `URI`+`Name`).

### G13 [закрыт, `864ab6b16`] `read_mcp_resource`: бинарный blob без лимита и MIME

`read_mcp_resource.go:85-87` `string(content.Blob)` — сырые байты, без
кэпа ни здесь, ни в `Registry.ReadResource` (`resources.go:52-62`).
`fetch` режет 5 MiB, `read` — 2000 строк. PDF на 20 МБ → невалидный
UTF-8 в SQLite и в каждый ход. Правка: по `MIMEType` (текст / картинка
через `NewImageResponse` / отказ с размером), байтовый лимит с
`truncated`; ту же границу — текстовому результату `RunTool`.

### G14 [закрыт, `168d80daf`] Halt хука не останавливает ход у порога окна контекста

fantasy считает `StopWhen` независимо от `stopTurnRequested`
(`third_party/fantasy/agent.go:577,616-618`), `onStepFinish` пишет
`EndTurn` (`turn.go:775-782`), но `finishTurn` смотрит только
`len(ToolCalls()) > 0` (`run_turn.go:430-446`) и делает
`requeueContinuation` с «The previous session was interrupted…».
`hooked_tool.go:65-71` обещает «Halt ends the whole turn»; у порога окна
halt/deny превращается в «суммаризировать и продолжить». Правка:
`t.haltedByTool` в `onStepFinish`, без requeue при нём.

### G15 [закрыт, `168d80daf`] Отрицательное окно контекста проходит в арифметику

Хвост D2. `summarizePolicy.window` считает «нет окна» при `<= 0`
(`usage.go:46-51`), `stopOnContextWindow` — только `cw == 0`
(`turn.go:855-859`). При `cw < 0`: `cw/2`, `remaining < threshold`
всегда → `shouldSummarize` на каждом шаге, summarize + requeue по кругу.
Источник после D2 — только конфиг: `model --context-window -1`
(`shellconfig/model.go:66`, `flags.go:136` знак не проверяет). Правка:
`cw <= 0` и отказ отрицательного в `shellconfig`.

### G16 [закрыт, `168d80daf`] На ошибке провайдера UI получает и «finished», и «failed»

`completeTurn` публикует `TypeAgentFinished` и при `err != nil`
(`run_turn.go:495-501`, из error-path `:851`); `AgentDispatcher.run`
затем публикует `TypeAgentError` (`app/agent_dispatch.go:170-176`).
`ui/model/notifications.go:219-244` шлёт оба уведомления. Правка:
`AgentFinished` только при `err == nil`.

### G17 [закрыт, `168d80daf`] Отмена до создания assistant-сообщения не дренирует очередь

`run_turn.go:825-827` при `currentAssistant == nil` возвращается без
`drainNext`, комментарий обещает, что сюда ведёт только откат
`foldSteering`. Но `handleStreamErrorBeforeAssistant` (`turn.go:959-966`)
ведёт сюда же: подготовка до `Stream` (`sessions.Get`, `List`, два
`preparePrompt`) идёт на `ctx`, и `Cancel` в это окно даёт этот путь.
Промпт, набранный после Escape, остаётся в очереди — `wakeFromInboxIfIdle`
её не смотрит, UI показывает «1 queued» на простаивающей сессии.
Правка: дренировать на cancel-пути; откат `foldSteering` отличим по
`!isCancelErr`.

### G18 [закрыт, `f0676ae0f`] `UpdateSessionUsage` пишет `title` из снимка

Комментарий у `SaveUsage` (`service.go:40-48`) называет конкурента —
асинхронный заголовок (`title.go:212`) — и защищает `cost` через
`cost = cost + ?`, но `sessions.sql:87` ставит `title = ?` из
`currentSession`, прочитанного в `summarizeMessages` (`usage.go:214`) до
потока провайдера. Суммаризация сразу после первого хода (большие
вложения) ложится поверх заголовка; `generateTitle` больше не запускается
(`run_turn.go:715`) — «New Session» навсегда. Правка: убрать `title` из
запроса; никто из вызывающих `SaveUsage` не переименовывает.

### G19 [закрыт, `f0676ae0f`] `Rename` обещает не трогать `updated_at`, триггер трогает

`service.go:336` против триггера из
`20260811000001_preserve_explicit_session_updated_at.sql:10-14`: условие
`NEW.updated_at = OLD.updated_at` не отличает «не задан» от «задан тем
же». Проверено в sqlite3. `sennit session rename` старой сессии делает её
«последней» для `GetLastSession`, сбрасывает возраст для `gc` и раздувает
`time_seconds = updated_at - created_at` в `ProjectStatsSince`. Правка:
убрать триггер, явный `updated_at` у писателей, которым он нужен
(`UpdateSession`, `UpdateSessionUsage`, `AttributeTaskCostOnce`,
`SetSessionModel`). Дешёвая альтернатива — переписать комментарий.

### G20 [закрыт, `23a17963d`] Запись через висячий симлинк: диалог подписан путём ссылки

`filemutation.go:158-167`: `resolveExistingAncestorSymlinks` на висячей
ссылке падает (намеренно, `tools.go:243-247`), ошибка гасится в
`resolvedFilePath = req.filePath`, `Path` схлопывается в `workingDir`,
описание «Create …/latest.log». Запись идёт в `writePath` из
`ResolveWriteTarget` (`:148`) с `MkdirAll` у цели (`:179`) —
`/var/app/current.log`. Класс F4. Грант точечный (JSON с содержимым в
ключе), остаётся ложная подпись. Правка: при ошибке резолва брать
`writePath` и «(resolves to …)», как `outsideWorkdirNotice`.

### G21 [закрыт, `23a17963d`] `download` не резолвит симлинки-предки в ключе разрешения

`download.go:106-115` `Path: filePath` без `resolveWithinWorkdir` и
`outsideWorkdirNotice`, которыми F4 закрыт для `read_core.go:44-48` и
`applyFileMutation`. Сценарий из `tools.go:227-231`: `ln -s ../.. up` →
`download <url> up/x` → диалог «to …/up/x», файл на два уровня выше.

### G22 [закрыт, `864ab6b16`] Потеря владения MCP-сессией обрывает батч

`tools/mcp/init.go:33,61,66,70` и `resources.go:37` возвращают
`context.Canceled` вместо `errLostOwnership`; `mcp-tools.go:185` по
`errors.Is(err, context.Canceled)` отдаёт Go-ошибку — батч обрывается
при `ctx.Err() == nil` (ленивый реконнект упавшего stdio-сервера
совпал с правкой `sennitrc`). Две ветки для одного смысла. Правка:
единый `errLostOwnership`; `connection.go:526` учитывать его.

### G23 [закрыт, `0ccc91741`] `home.Long` склеивает `~user/…` с чужим домом

`home/home.go:60` проверяет `HasPrefix(p, "~")`, не `"~/"`:
`~alice/bin/server` → `/home/bobalice/bin/server`. Вызывающие — команды
MCP (`transport.go:68`), LSP (`lsp/lifecycle.go:97`), скиллы
(`skills/manager.go:290`), контекстные файлы (`prompt.go:160`),
`fsext/ls.go:82`. Правка: расширять только `~` и `~/`.

### G24 [закрыт, `864ab6b16`] `ErrAllExhausted.ResetsAt` считает выключенные аккаунты

`providers/accounts/rotator.go:260-270` берёт `earliestResetFor` и для
`Disabled`; уведомление «resets at 15:04» указывает на момент, когда
ничего не изменится. Правка: пропускать `a.Disabled`.

### G25 [закрыт, `23a17963d`] `fetch` валидирует `timeout` после диалога разрешения

`fetch.go:87-104` — диалог показан, одобрен, затем «timeout must be
between 0 and 120»; `download.go:85-87` проверяет до. Перенести выше.

## Фаза 2. Границы контекстов

### B1 [закрыт, `0ccc91741`] `internal/commands` импортирует `internal/agent/tools/mcp`

`commands/commands.go:13`; `LoadMCPPrompts(*mcp.Registry)` (`:93`) и
`GetMCPPrompt(*mcp.Registry, mcp.ConfigProvider, …)` (`:250`).
Единственный потребитель — `appws/app_workspace_mcp.go:74,79`, у которого
уже есть `w.app.MCP` и `w.store`. Пакет, читающий `.md` из каталогов,
тянет реестр MCP-клиентов с транспортом и процессами. Инверсия без
цикла: `mcp.Prompt` — alias SDK-типа (`tools/mcp/prompts.go:11`), так что
`LoadMCPPrompts` может принимать `iter.Seq2[string, []*sdkmcp.Prompt]`, а
`GetMCPPrompt` — замыкание; appws передаёт `w.app.MCP.Prompts()`.

### B2 [закрыт, `0ccc91741`] Первое нативное уведомление пишет иконку на диск в `Update`

`ui/notification/native.go` `Send`: `resolvedIcon()` → `CacheIcon`
(`icon_cache.go:27-34`: `ReadFile`, `MkdirAll`, `WriteFile`) выполняется
синхронно до возврата `tea.Cmd`; вызывается из `UI.sendNotification`
(`model/notifications.go:44`) внутри `Update`. Один раз за процесс, но
это ровно то IO, которое `ui/AGENTS.md:9` запрещает. Правка: перенести
внутрь замыкания.

### B3 [L, не делать наспех] Продолжение припаркованной делегации выполняет агент-кодер

Задача делит App родителя; пробуждение припаркованной сессии идёт
`Coordinator.DeliverTaskCompletion` → `agentPort.current()` →
`turnDispatcher.runContinuation` (`turn_dispatcher.go:168-196`):
`runtimeFor` собирает рантайм верхнего агента и запускает
`d.agentPort.current().Run`. У той же сессии между первым и вторым
раундом меняются: системный промпт (`coder.md.tpl` вместо
`task.md.tpl`), набор инструментов (`agent` с ростером именованных
агентов, что `delegation_finalizer.go:908-914` прямо запрещает делегату;
`question`; `thread_*`), PreToolUse-хуки (вопреки `hooked_tool.go:32-34`
«sub-agents never fire hooks»), `NonInteractive` (уведомление «Task
finished: <делегация>» на каждый раунд), todo-reminder в промпте.
Тестов на то, *кем* выполняется продолжение, нет; `beea7376a`
закрепляет только *что* оно финализирует парковку.

Правка — хранить у делегации её `SessionAgent` и запускать его с
`NonInteractive: true`. Риск: sub-agent пересобирается при инвалидации
рантайма — нужны `waitReady` и защита от устаревшего экземпляра. Сначала
тест, фиксирующий текущее поведение как дефект, потом правка.

### B4 [design] Делегации, суммаризация и заголовок не участвуют в ротации аккаунтов

`OnRateLimit`/`RotateThreshold` подключаются только в `makeRunCall`
(`turn_dispatcher.go:214-221`); `buildSubAgentCall`
(`delegation_finalizer.go:786-804`) и `Summarize` (`:131`) дают лишь
`OnAuthRefresh`. С запасным аккаунтом 429 в делегации падает исходным
429, родительский ход переключился бы. Документация
`SessionAgentCall.OnRateLimit` перечисляет причины `nil` без «это
делегация». Решить: подключать или задокументировать.

## Фаза 3. DRY, KISS, мёртвые механизмы

### D1 [закрыт, `23a17963d`] Шлюз «вне рабочей папки» скопирован в пять читающих инструментов

`glob.go:80-102`, `grep.go:173-195`, `ripgrep.go:120-142`, `ls.go:104-126`,
`read_core.go:50-60` — одинаковы вплоть до обработки ошибок, различаются
именем инструмента, действием, текстом и `Params`. Цена показана, не
предположена: `99efb4886` (F4) правил все пять одним и тем же diff'ом по
семь строк. Хелпер `requireOutsideWorkdirPermission(ctx, perms, call,
toolName, action, verb, abs, resolved, params)`; `web_fetch.go:46-67` и
`web_search.go:99-120` — тот же блок без резолва пути, можно тем же
хелпером с пустым `resolved`.

### D2 [закрыт, `0ccc91741`] `matchedRanges` и `bytePosToVisibleCharPos` продублированы между UI-пакетами

`ui/completions/item.go:312-355` и `ui/dialog/sessions_item.go:236-278` —
43 строки побайтово, uniseg-логика ширины графем. Место — `ui/util` или
`ui/common`, оба уже в графе обоих пакетов.

### D3 [закрыт, `0ccc91741`] `RuntimeStore.WriteRuntimeConfigFields` — без вызывающих вне тестов

`config/runtime.go:57`, реализация `store.go:406` с doc-комментарием;
потребители — `watch_test.go:268` и заглушка
`providerload/loader_test.go:25`. `providerload` пользуется только
`RemoveRuntimeConfigField` (`loader.go:113`). Пятый мёртвый механизм с
живым описанием, найден тем же вопросом «есть ли вызывающий вне
тестов» (единственный из 515 методов интерфейсов, кроме sqlc-шного
`PrepareContext`). Удалить метод из интерфейса; реализацию — если
`watch_test` не проверяет ею что-то, что нельзя проверить иначе.

### D4 [закрыт, `168d80daf`] Мёртвое описание в `turnDispatcher.run`

`turn_dispatcher.go:263-299` описывает «coalesce per-attempt RunComplete»
и «the auth-retry chain may call run twice», ради чего `onDispatch`
обёрнут в `sync.Once`. `run()` вызывается один раз (`:317`); retry живёт
внутри fantasy. Механизм безвреден, описание вводит в заблуждение —
класс 2. Убрать `Once` и абзац.

### D5 [закрыт, `23a17963d`] Отмена в четырёх LSP-инструментах уходит модели текстом

`lsp_replace_symbol.go:93-95`, `lsp_symbols.go:45-47`, `lsp_hover.go:74-76`,
`lsp_call_hierarchy.go:52,66,81` заворачивают любой `err`, включая
`context.Canceled`, в `NewTextErrorResponse`; `lsp_definition.go:51`,
`references.go:48`, `lsp_rename.go:62` через `isGenuineSymbolMiss`
делают правильно, и `lsp_helpers.go:25-35` формулирует это как правило.
Класс E7: по Esc в транскрипте «failed to get document symbols: context
canceled» как ошибка инструмента. Правка: `if ctx.Err() != nil { return
…, ctx.Err() }` перед текстовым ответом.

## Проверено и чисто

Записано, чтобы не читать второй раз.

- **Граф импортов.** Замыкание `internal/ui` не содержит `db`, `agent`,
  `app`, `shell`, `lsp`, `hooks`. Листовые пакеты (`proto`, `message`,
  `session`, `skills`, `stats`, `permission`, `question`, `csync`,
  `pubsub`, `env`, `home`, `fsext`, `git`, `diff`, `log`, `spin`,
  `latency`, `toolmeta`, `clipboard`, `filetracker`, `history`, `oauth`)
  не импортируют оркестрацию. Единственное нарушение — B1.
- **Треды.** Реле разрешений (`forwardPermissions` на `watchCtx`,
  `PermissionsFor`+`answerPermission` пробуют оба сервиса); все воркеры
  через `goWorker`, `Shutdown` отменяет `m.ctx` до `wait()`; `unwinder` в
  Create/Activate/send; `toProto` переносит всё, кроме outbox-полей задач,
  у которых нет читателей в UI. Внутри треда делегаций нет — все три
  гейта false.
- **MCP.** `publishMu`-дисциплина, `beginRenewal`, `teardown` ждёт записи
  токена; `tokenwrite` без двойного refresh; `channel.go` и `schema.go`
  с лимитами; секреты не попадают в лог. `credentials.Manager`:
  singleflight + межпроцессный lock, адопция свежего токена с диска.
  `codex`/`copilot`/`callback`: PKCE, `state` до `error`, device flow с
  `slow_down`, `html/template`.
- **Хранилища.** Мёртвых SQL-запросов ноль — восьмёрка из аудита 9 уже
  удалена, у каждого из 60+ методов `querier.go` есть вызывающий вне
  тестов. JSON частей сообщения симметричен по всем восьми типам.
  `read_at` в миллисекундах согласован у единственного писателя и всех
  читателей. Миграции аддитивные. GC защищает нетерминальные делегации и
  идёт BFS по `parent_session_id`. Буфер сообщений: «сначала стоп, потом
  дренаж». sqlc не течёт в доменные пакеты; единственное касание —
  `appws/persist_message.go` через `db.IsForeignKeyConstraintError`, без
  показанной цены.
- **Ядро хода.** Ledger очереди, `drainNext`/`requeueDrained` по seq;
  осиротевшие tool calls закрываются `closeUnfinishedToolCalls` и чинятся
  `preparePrompt` с обеих сторон; `trimToSummary` не переупорядочивает;
  `runtime_cache.getOrBuild` не усыновляет чужой cancel; ротация без
  цикла на `ErrAllExhausted`; хуки накладываются после MCP-инструментов;
  `completion_inbox` at-most-once с ограничением хопов.
- **Инструменты.** Все четыре мутатора пишут через `ResolveWriteTarget`
  (цель, не ссылка); ключ `filetracker` через `fsext.Canonical`; E1/E5/F2
  держатся для `write` и `multiedit`; ранняя граница рабочего каталога у
  всех мутаторов; SSRF C1 — единственный HTTP-клиент с
  `checkFetchRedirect` и восстановленным лимитом редиректов; фоновые
  задания: `Cleanup` только завершённых старше 8 ч, `SyncBuffer` 512 KiB.
- **Листовые.** Ротация лога (10 МБ, 30 дней, sweep пропускает `-panic-`
  и свой файл); `EarlyHandler` под одним мьютексом;
  `HTTPRoundTripLogger` редактирует `Authorization`, `x-api-key`,
  `access_token`; `clipboard` без OSC52, `Init` до первого `WriteText`;
  `proxyhttp` — пустой прокси уважает `NO_PROXY`, ошибка парсинга не
  превращается в «без прокси»; `dns` мутирует `DefaultResolver` в одном
  месте по `TERMUX_VERSION`; `migrate` идемпотентна; `spin` без паник по
  индексу при смене длины label.
- **UI.** Незарегистрированный инструмент в реестре разрешений идёт в
  `renderDefaultContent` — JSON-дамп, не пустой экран; ложное `ok` в
  type assertion невозможно по транспорту. Остальные пять путей вложений
  капируют 5 МБ, stat/stat без симлинк-асимметрии. `loadFiles` только
  внутри `Open`-cmd. Очереди уведомлений нет. `listcache` со stale
  generation корректен.

## Отклонено при перепроверке

Гипотезы, не пережившие чтение потребителей; записаны, чтобы не
поднимать снова.

- `stdioCheck` перезапускает stdio-команду с побочными эффектами — 5 с,
  `stdin=/dev/null`, группа убивается; вреда не показано.
- `isOAuthInitErr` матчит подстроки — нужен сервер с `invalid_grant` в
  теле 5xx, не показано.
- `mergeCatalogProviders` удаляет `providers.anthropic` при `oauth` —
  намеренная миграция с тестом.
- `Delete` во время таймерного флаша публикует `Updated` после `Deleted`
  — UI для неизвестного ID элемент не создаёт.
- Порядок сообщений при равном `created_at` — обход индекса отдаёт
  порядок вставки.
- Steering в чужую сессию после делегации — очередь ключуется
  `call.SessionID`.
- `registerThreadParent` после `startRun` в `Manager.send` — окно есть,
  доставка идёт по персистентной колонке; не воспроизводимо.
- `Cancel` на idle-треде — человек нажал сам; отказа нет.
- Гранты переиспользуются на висячей ссылке — нет, ключ содержит
  `NewContent`.
- `lsp_rename` пишет историю до `ApplyWorkspaceEdit` — лишняя версия
  равна диску, undo не страдает.
- `modelcache.Load` false на ошибке затирает метаданные — требует
  `SQLITE_BUSY` при `busy_timeout=30s` и одном соединении.
- `sweepStaleLogs` удалит активный лог соседа — только при 30 днях без
  записи.
- `OAuthCodex.currentProxy` читает диск в `HandleMsg` — нет, уже вынесено
  в cmd.
- `QuestionAnswer` выбрасывает bool — любая `question.Notification`
  закрывает форму.
- Клоны из детектора, которые **не** DRY: per-vendor построители
  провайдеров (`providers_build.go:161-189`, разные типы `Option`),
  `shellconfig/permissions.go:48-84` (два builtin'а по 15 строк),
  палитры тем.

## Что дал этот аудит методически

**Правка одного пути из трёх — не правка.** G1 (грант только в `Create`),
G7 (логгер только в `setupLocalWorkspace`), G11 (`Stat` только в
`Activate`), G21 (`outsideWorkdirNotice` не в `download`), D5 (три
LSP-инструмента из семи) — один и тот же дефект: инвариант закрыт на
одной поверхности запуска и не закрыт на соседних. Перед закрытием
находки перечислять **все** пути, ведущие к тому же состоянию, и
проверять каждый. F1/F7 вчера закрыли `Create`; респаун — тот же вход.

**Документ — тоже механизм с живым описанием.** 5.3 стоял открытым,
хотя код уже был исправлен. Проверять статус «открытых с причиной»
пунктов тем же грепом, что и код.

**Детектор клонов полезен как указатель, не как находка.** Из
шестнадцати пар только две стали D1 и D2 — те, где цена показана
коммитом или где копия уже живёт в двух пакетах с общим соседом.
Остальные — законная параллельная структура.
