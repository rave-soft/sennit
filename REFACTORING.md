# План рефакторинга

Только открытые пункты. Убирайте пункт, когда он закрыт; история остаётся
в git. Внутри фазы задачи независимы и могут идти параллельными коммитами.

Условные обозначения: **[S]** малая правка (≤ 1 файл, ≤ 1 час),
**[M]** средняя (несколько файлов, тесты), **[L]** структурная (новый пакет
или перенос ответственности).

## Решения первого аудита (2026-09-02, bdc47daab), которые стоит помнить

Все семь фаз закрыты. Три вывода остаются в силе:

**Кеширование окружения конфигурации отклонено, не отложено.** Записи
`env` резолвятся лениво против текущего окружения, и это зафиксировано
двумя тестами: повтор после ошибки авторизации и
`TestLoadRuntimeResolverReadsCurrentEnvironment`. Попытка закешировать
сломала оба. Проблема реальна — запись с `$(cmd)` выполняет команду на
каждое разрешаемое значение — но лечится не кешированием целиком.

**Три места в `internal/workspace` остаются на своих местах.** Перенос
`resolve_session.go`, `session_changes.go` и `custom_provider.go` в
`appws` дал бы цикл импорта или заставил бы UI импортировать бэкенд
напрямую.

**Ранняя проверка границ рабочей папки в мутирующих инструментах не
избыточна.** Без неё сначала срабатывают stat и проверка свежести, отказ
приходит с другим текстом, а файлы вне папки читаются до отказа.

---

# Аудит 2 (2026-09-02, `main`, b04a0e592)

Критерии: SOLID, DRY, KISS, YAGNI, границы контекстов. Каждая находка
подтверждена чтением кода; статистика (граф импортов, длина функций,
плотность комментариев) снята скриптами по `internal/`, без
`third_party`.

## Фаза 1. Баги и подозрительные места

### 1.1 [отклонён] Непроверенный `Close` при скачивании

Находка ошибочна. `errcheck` указывает на `outFile.Close()` в `defer`
(`download.go:187`), но это путь очистки: файл там всё равно удаляется по
`cleanupTmp`. Успешный путь закрывает файл явно и проверяет ошибку
(`download.go:204`) до `fsext.ReplaceFile`. Порядок «скопировать →
закрыть с проверкой → переименовать» уже верный, менять нечего.

## Фаза 2. SOLID

### 2.1 [отклонён] `config.ConfigStore` — god-объект

71 метод — правда. Но пакет не режется, и это проверено двумя разными
гипотезами, а не одной.

**По данным (credentials) — не режется.** Запись креденшела *и есть*
запись конфига: она публикует новый `*Config` под тем же `writeMu`, что
и любой мутатор. Вынесенный тип всё равно принимал `*ConfigStore`
параметром ради восьми чужих полей — два получателя на одну сущность
плюс шесть форвардеров. Откачено; причина записана в шапке
`credential_store.go`.

**По времени жизни (reload/watch/staleness) — тоже не режется.**
Гипотеза была, что эта группа работает с файлами на диске под своим
`reloadMu`, а не с `*Config` под `writeMu`. Неверно: `reloadFromDisk`
берёт `writeMu` на чтение (`reload.go:33,54`) и на полную запись для
финальной подмены (`reload.go:123`), а `staleness.go:200` прямо требует
от вызывающих держать `writeMu`, если они могут гоняться с публикацией.

**Общий вывод, который стоит помнить.** `ConfigStore` — не свалка
несвязанных обязанностей, а один опубликованный снимок с множеством
способов его обновить. Всякий путь, что бы ни делал по дороге,
заканчивается `setConfig` под `writeMu`. Резать по любому признаку —
данные, время жизни — значит оставлять новому типу зависимость от
исходных локов. Пока `*Config` публикуется целиком, 71 метод — это
цена, а не небрежность.

Что тут действительно хорошо сделано и что не стоит сломать: перезагрузка
держит `writeMu` только на подмену, а дисковый ввод-вывод и HTTP
выполняет вне лока — это зафиксировано тестом
`TestReloadFromDiskLocked_DiscoveryDoesNotBlockWriteMu`.

### 2.2 [L, частично закрыт] `agent.delegationFinalizer` делает всё

Skills-состояние и HTTP-клиент fetch-инструмента вынесены в свои типы
(`skillsState`, `fetchClient`); пять мьютексов/атомиков стали три.
Осталось: `subSessions`, `delegationTools`, `runtimeInputsCache` — они
про делегирование по существу, и разрезать их дальше стоит только если
появится причина, а не ради счёта полей.

### 2.3 [закрыт] `ui/model.UI`

Диспетчеры разобраны (`947077b3f`), `keyMapForPlatform` тоже
(`df7377d09`). По самой структуре — дальше без передела не идёт, и это
измерено, а не оценено на глаз.

Из 35 полей 29 уже подструктуры (`sess`, `editor`, `lay`, `sidebar`,
`queued`, `wsCache`, `promptQueue`, `panel`, `lsp`, встроенные `widgets`,
`term`, `mouseState` и прочие). Оставшиеся шесть — `com`, `embedded`,
`focus`, `state`, `keyMap`, `goos` — сквозные по существу: общий handle
и состояние, которое маршрутизирует сообщения и открывает каждый экран.

Подсчёт транзитивного замыкания по группам для каждого метода показал,
что почти все трогают две и больше групп по замыслу: диспетчеры,
отрисовка и обработчики законно нуждаются в `com` плюс состояние.
Однозначно односоставных нашлось четыре, они и переехали на свои
структуры (`dfe8b5758`). Остальное требует передела, а не перекладывания.

### 2.4 [отклонён] `readOnlyWorkspace` написан руками

799 строк и 120 методов, из них 54 форвардят в `w.ws`, — но встраивать
`Workspace` нельзя, и это уже осознанное решение, описанное в шапке файла
(`read_only_workspace.go:45-69`). Без встраивания каждый метод контракта
обязан быть реализован явно, иначе падает `var _ Workspace =
(*readOnlyWorkspace)(nil)`. Это default-deny: новый мутирующий метод
контракта — ошибка компиляции, а не молчаливое проксирование в живой
workspace. Встраивание обменяло бы ошибку компиляции на тихую дыру.
Полноту классификации сторожит
`TestReadOnlyWorkspace_MethodClassificationIsComplete`. Форвардеры —
цена default-deny, а не небрежность.

### 2.5 [закрыт] Контракт `FrontendWorkspace` неоднороден

Десять «голых» методов разложены по ролям (`OAuthController`,
`PreferredModelUpdater`, `ConfigFieldEditor` плюс новые
`ProviderCatalog` и `AccountUsage`); в теле `FrontendWorkspace` не
осталось ни одного. Набор методов контракта не изменился — 118 имён до и
после, ни одна реализация и ни одна тестовая заглушка не тронуты.

**Утверждение аудита про семь однометодных `Account*` было неверным.**
Они не «без потребителей»: все семь берутся в `internal/cmd` как типы
параметров и поля локальных составных интерфейсов —
`recordCopilotAccount(ws workspace.AccountRecorder, …)`,
`findAuthAccount(ws workspace.AccountLister, …)` и так далее. Роль с
настоящим узким потребителем — это то, ради чего роли и заводят;
сворачивать их было бы регрессом. Проверять потребителей **до**, а не
после.

### 2.6 [частично закрыт] `shutdownPhases.Shutdown`

Пять одинаковых циклов по очередям хуков сведены к `runHookList`
(`fc2361e51`). **Таблица фаз отклонена.** Остальная последовательность не
раскладывается на однородные записи: шаг остановки агента ветвится между
тестовым сидом и путём через координатор, параллельный шаг запускает две
горутины со втиснутым между ними закрытием herdr, а `mcpClose`,
`mainDBRelease` и `messages.Close` — одиночные действия под флагами,
вычисленными по ходу. Таблица потребовала бы пяти escape-hatch'ей и
читалась бы хуже явной последовательности. Заодно закрыта дыра в
покрытии: относительный порядок `preCleanupHooks` и `shutdownHooks` не
был зафиксирован ни одним тестом.

## Фаза 4. KISS

### 4.1 [закрыт] Комментарии рассказывают историю багов вместо кода

Пройдены все шесть крупных пакетов: `thread` (17 %), `ui/dialog`,
`ui/chat` (2 %), `agent` (1 %), `config` (1 %), `ui/model`.

**Отношение комментариев к коду — негодная метрика, не опирайтесь на
неё.** Исходная оценка «минус треть» выведена из неё и завышена на
порядок. Три прохода подряд показали одно и то же: большая часть
комментариев несёт рассуждение, которого в коде нет, и целыми файлами
править нечего — в `ui/chat` пятнадцать файлов из тридцати трёх не
потребовали ничего, в `thread` семь из двенадцати, в `agent` тридцать
один. Настоящая цель — историческая наррация, и её на порядок меньше,
чем показывает отношение.

Как это делать, чтобы не навредить:

- **Никаких числовых нормативов.** Первая попытка задала «минус треть»,
  и это немедленно превратилось в вырезание объяснений ради цифры.
  Единственный критерий — восстановима ли информация из кода;
  сомневаешься — оставь. Недорезать дешевле, чем срезать несущее.
- **Ни одной строки кода.** Проверяется командой:
  `git diff -U0 -- <pkg> | grep '^[-+]' | grep -v '^[-+][-+]' | grep -vE '^[-+]\s*(//.*)?$'`
  должна печатать пусто.
- **Не гоняться за подстрокой.** «used to» чаще всего значит
  «используется, чтобы», а «no longer» — состояние рантайма, а не
  прошлое кода. Список исключений выдавать вместе с заданием, иначе
  проход сломает нормальные комментарии.
- Убирать: историю («used to be», «the old code», «before this was
  extracted»), пересказ следующей строки, одно и то же рассуждение у
  хелпера и у каждого его вызова, пять предложений на двухстрочный
  guard, указатели на документы, которых нет в репозитории.
- Оставлять: почему сделан неочевидный выбор, инвариант для вызывающего,
  перекрёстную ссылку на место, где значение читают обратно,
  предупреждение о ловушке, ответ на «почему это экспортировано», и —
  в UI — всё про ширину терминала, ANSI и переносы.
- **Прошлое лекарство переписывать в живое предупреждение.** Не «раньше
  это обходили через `os.Setenv`», а «в отличие от `os.Setenv`, которая
  испортила бы ключ всем остальным провайдерам». Опасность сохраняется,
  рассказ уходит.
- Результат «этот файл уже в порядке» — валидный.

Попутная польза, которую стоит ожидать: проход по `ui/chat` нашёл три
дефекта документации — `MCPToolRenderContext` был описан как рендерер
bash-сообщений, и три doc-строки задублированы склейкой при слиянии.

### 4.2 [закрыт] Длинные функции

Разобраны все: `updateMouse` 347 → 15 и `updateSession` 341 → 37
(`947077b3f`), `summarize` 285 → 33 и `getProviderOptions` 265 → 32
(`e9c88512a`), `QuestionForm.Draw` 251 → 10 (`950de0598`),
`keyMapForPlatform` 250 → 44 (`df7377d09`), `Shutdown` (`fc2361e51`),
`runTurn` 96 → 31 (`0580c1b90`). `scanBackward`
(`agent/tools/sennit_logs.go`, 219) **отклонён** — см. 5.6.

Что оказалось важным при разборе рендерящих и диспетчерских функций:
проверять не «стало короче», а конкретные инварианты. В `Draw` — что
путь confirm-таба по-прежнему возвращает курсор без поправки, а путь
вопроса с ней. В `keyMapForPlatform` — что карта привязок берётся один
раз и `overrides` применяются к ней в той же точке. В диспетчерах UI —
что `break` внутри `case` превращается в `return` только там, где после
него ничего не дописывается в `cmds`. И golden-файлы обязаны проходить
**без** `-update`: расхождение там означает изменение поведения, а не
повод их перегенерировать.

## Фаза 5. YAGNI и границы контекстов

### 5.1 [отклонён] `internal/proto` охраняет дублирование

Находка неверна целиком. Она выведена из числа строк, имени пакета и
наличия файлов `*_parity_test.go` — при поиске потребителей я искал
префикс `proto.` и не увидел главного: **тридцать файлов `internal/ui`
импортируют этот пакет под псевдонимом `tools`**
(`tools "github.com/rave-soft/sennit/internal/proto"`). Именно ради них
существует основная масса пакета: рендерер чата декодирует сохранённый
JSON вызовов инструментов, не втягивая тяжёлый `internal/agent/tools`.

Ни одного неиспользуемого экспортированного символа в пакете нет.
Проверено посимвольно, включая типы, которые нигде не названы по имени
снаружи (`MultiEditOperation`, `FailedEdit`, `QuestionChoice`,
`ReadResourceType`), — каждый является типом поля живого соседа.
Пробные удаления немедленно ломали сборку `internal/ui/chat`.

**Parity-тесты не охраняют дублирование, а проверяют полноту
конверсии.** Их собственный комментарий говорит прямо: копия — это
замысел, потому что `proto` намеренно не зависит от `internal/hooks`.
Тест ловит реальный отказ: поле, добавленное в `hooks.HookInfo` и
забытое в `proto.HookInfo`, — не ошибка компиляции, так как `internal/
agent` копирует поштучно, и транскрипт молча перестаёт показывать то,
что раннер знает. `lsp_parity_test.go` тем же способом ловит новое
состояние LSP, проваливающееся в `default`.

### 5.2 [отклонён как сформулирован] `internal/config` — хаб

Три независимые причины, каждая достаточна сама по себе.

**Посылка неверна.** `Config()` возвращает не стор, а опубликованный
снимок-значение: `ConfigStore.Config()` отдаёт `s.config` под RLock, а
`cloneForWrite` глубоко копирует всё, что трогает мутатор, ровно чтобы
чтение снимка не брало лока. Мьютексы, перезагрузка и путь записи наружу
не видны. Формулировка «UI получает весь стор» ложна.

**Инструмент выбран неверно.** DTO пришлось бы держать в согласии с
пятнадцатью полями `config.Config`, который прирастает полями при каждой
второй фиче, и забытое поле в конвертере не ловится ничем — экран просто
тихо отрисует ноль. Parity-тесты `proto` тут не помогут: они держат
замкнутые перечисления, а не состав структуры. И `AGENTS.md` прямо
запрещает заводить DTO «на всякий случай».

**В UI уже есть правильный приём, и он не DTO.** `internal/ui/chat` не
принимает `*config.Config` вовсе: `ModelConfig` (`messages.go:338`) и
`CustomAgentConfig` (`tools_core.go:250`) — узкие интерфейсы на два-три
метода, которым `*config.Config` удовлетворяет неявно. Продакшн-код
`ui/chat` не импортирует `internal/config` совсем.

**Выигрыша по зависимостям не было бы.** `internal/workspace` называет
`config.Scope` в `SetConfigField`, `RemoveConfigField`, `SetCompactMode`
и `RecordAccount`, а `config.Problem` — в `ConfigProblems`. Замена
одного `Config()` на DTO не убирает `internal/config` и его 28
транзитивных пакетов из линковки TUI. Тот же урок, что и в 5.3.

**Счёт «22 импортёра» — шум.** `lsp` объявляет свой узкий
`ConfigProvider`; `projects` зовёт один хелпер пути; `importer` и
`doctor` используют типы-значения и константы. Ни один не трогает
поведение стора.

Что остаётся сделать, если захочется: завести такие же узкие интерфейсы
в `ui/model` и `ui/dialog`, где сосредоточены почти все 24 обращения.
Это инкапсуляция и читаемость, **не** уменьшение связности — продавать
это как выигрыш по зависимостям нельзя.

**Найдено попутно и стоит помнить:** неизменяемость снимка держится на
соглашении, а не на типах. `Config.Providers` — `*csync.Map`, ссылочный
и мутабельный: держатель `*config.Config` может изменить его на месте в
обход clone-and-swap. Продакшн так не делает,
`ui/dialog/accounts_test.go:387` — делает.

### 5.3 [переформулирован] В контракте `workspace` живут побочные эффекты

Исходная формулировка («импортирует 15 пакетов, `MaxBackgroundJobs`
возвращает `shell` в UI транзитивно») проверку не прошла. Классификация
всех пятнадцати по фактическому использованию:

- **11 — чистые DTO**, ради которых контракт и существует: `message`,
  `session`, `history`, `oauth`, `permission`, `proto`, `question`,
  `skills`, `stats`, `providers/accounts` и `config` (последний тяжёлый,
  но несущий: `config.Scope` принимает почти каждый метод записи).
- **3 — поведение**: `git.UncommittedFiles` запускает подпроцесс
  `git status` (`read_only_workspace.go:355`); `discover.DiscoverModels`
  ходит по HTTP (`custom_provider.go:59-66`); `diff.GenerateDiff` —
  чистое вычисление, безобидно.
- **1 — случайность**: `shell` ради одной константы. Убрано
  (`185b43f14`), значение продублировано с тестом
  `TestMaxBackgroundJobsMatchesShell` против расхождения.

**Транзитивно это ничего не дало, и рассчитывать на такое не стоит.**
`internal/config` сам тянет `internal/shell` через `shellconfig` и
`hooks`, поэтому замыкание контракта — 41 пакет и до, и после, а
`internal/shell` в нём остаётся.

**Подпроцесс убран** (`8bef12925`): `readOnlyWorkspace` получает
`uncommittedFiles` инъекцией, `appws/threads.go` передаёт туда
`git.UncommittedFiles`. Ни один продакшн-файл контракта больше не
запускает подпроцесс. Но это перенос поведения, **не** выигрыш по
зависимостям: `git.FileChange` остаётся в сигнатуре
`Workspace.UncommittedFiles`, замыкание — 41 пакет и до, и после.

**Сетевой вызов остаётся, и это упирается в жёсткое ограничение.**
`ConfigureCustomProvider` зовут напрямую из `ui/model/dialog_actions.go`
с `ws` типа `workspace.Workspace`. Инъекция discovery потребовала бы
метода в `customProviderConfigurer`, а раз вызов идёт через `Workspace`,
метод пришлось бы добавить и туда — то есть расширить контракт ради
сужения. Причина цикла в старой попытке выяснена точно:
`PrepareSessionChangesUsing` зовут и `appws.AppWorkspace`, и
`readOnlyWorkspace`, а последний обязан жить в `internal/workspace`, так
что перенос хелпера в `appws` заставил бы `workspace` импортировать
`appws`.

### 5.4 [частично закрыт] Docker MCP размазан по восьми пакетам

Зонд, TTL-кеш и процессный глобал вынесены в листовой
`internal/dockermcp` (`5976c13ef`), `internal/config/docker_mcp.go` — со
186 строк до 85. Мутаторы стора (`EnableDockerMCP`, `PersistDockerMCPConfig`
и прочие) остались в `config`: им нужны его потроха. `DockerMCPConfig()`
тоже остался — он возвращает `MCPConfig`, тип самого `config`, и переезд
развернул бы стрелку зависимости обратно. Рендер в `ui/chat`, `ui/dialog`
и `ui/model` не тронут — это отдельный пункт, если он вообще нужен.

### 5.5 [отклонён] `internal/agent` смешивает два контекста

Находка выведена из имён файлов и числа строк. Проверка по зависимостям
её не подтвердила: из пяти названных файлов ни один не является
провайдерным кодом, попавшим не в тот пакет.

- `credential_refresh.go` — 10 функций из 10 суть методы
  `*runtimeBuilder`; часть принимает `*activeRuntime` и возвращает
  `*compiledRuntime`.
- `providers_build.go` — 12 из 15 методы `*runtimeBuilder`, а сам
  `runtimeBuilder` тянет `agent/prompt`, `agent/tools`, `hooks`, `lsp`,
  `permission`, `question`, `session/store`, `shell`, `skills`.
- `rotation.go` — все функции нанизаны на `*activeRuntime` и
  `runtimeOperationPort`, который несёт живого агента координатора.
- `provider_log.go` — единственный без ссылок на оркестрацию, но
  неотделим на уровне неэкспортированных символов от `provider_stall.go`:
  `instrumentedModel.Stream` зовёт `newStreamStallWatchdog` и
  `isProviderStall` и читает тамошние константы таймаутов. Перенос
  потребовал бы экспортировать потроха сторожа через границу пакета.
- `compat.go` — вообще не про провайдеров: `preparePrompt`, починка
  осиротевших tool-результатов, напоминание про todo. Провайдерного там
  только `workaroundProviderMediaLimitations` и `cacheControlOptions`.

Импорты строго односторонние: `internal/agent` → `internal/providers/*`,
обратно ничего. Разделение 3.6 уже вынесло всё, что выносится без
затягивания графа оркестрации; оставшееся осталось потому, что ему нужны
`message`, `session/store`, `hooks`, `lsp`, `permission`, `skills` —
ровно то, от чего `internal/providers/*` правильно не зависит.

Единственное, что тут стоит поправить, — имя `compat.go`: файл
разнородный, и его название обещает провайдерные шимы, которых там
почти нет.

### 5.6 [отклонён] Сложность `sennit_logs` оправдана

Находка была выведена из размера файла (1357 строк) без чтения схемы и
тестов. Проверка это не подтвердила:

- каждый параметр `SennitLogsParams` документирован в `sennit_logs.md.tpl`
  и достижим из модели; схема и описание не разошлись;
- `scanBackward` — не редкая ветка, а единственный путь чтения: он
  вызывается на каждом успешном обращении (`sennit_logs.go:300`) и, сверх
  того, переиспользуется вторым инструментом (`agent_trace.go:144,162`);
- курсорная пагинация обещана в `<pagination>` и покрыта именными
  тестами на ротацию, устаревание, границы чанков и дозапись;
- 59 тестовых функций в трёх файлах, причём часть инвариантов
  `scanBackward` закреплена тестами, на которые ссылаются комментарии по
  имени.

Разбор `scanBackward` на именованные шаги тоже отклонён: его 219 строк —
в основном объяснения байтовых смещений и бюджета сканирования, а
дробление ради читаемости рисковало бы корректностью там, где её держат
эти самые комментарии и тесты.

## Что осталось открытым

Аудит отработан. Ниже — только то, что осознанно не сделано, с причиной.

**5.3, сетевой вызов в контракте.** `discover.DiscoverModels` ходит по
HTTP из `internal/workspace/custom_provider.go`. Заблокировано формой:
`ConfigureCustomProvider` зовут из `ui/model/dialog_actions.go` с `ws`
типа `workspace.Workspace`, поэтому инъекция потребовала бы нового
метода в самом контракте — расширить его ради сужения. Развязывается
только вместе с тем, как UI добирается до этой операции.

**5.4, рендер Docker MCP.** Зонд и кеш вынесены; отрисовка размазана по
`ui/chat`, `ui/dialog`, `ui/model`. Отдельный пакет под неё имеет смысл
только если появится второй такой вендор — сейчас это было бы
обобщение под один случай.

**2.2, остаток `delegationFinalizer`.** `subSessions`,
`delegationTools`, `runtimeInputsCache` — про делегирование по существу.
Резать дальше стоит по причине, а не ради счёта полей.

**`RuntimeProviders` не заморожен.** `Config.Providers` закрыт
(`6415f9b6f`): `setConfig` замораживает карту, мутация опубликованного
снимка паникует. `RuntimeProviders` — та же форма и та же уязвимость, но
прослежен и безопасен: `reload.go:151,180` работают с ещё не
опубликованным `built.cfg`, `provider_accounts.go:176` — внутри колбэка
`mutateInMemory`, который клонирует до вызова. Живой опасности нет,
заморозить стоит тем же приёмом.

**`config.NewStore` публикует без заморозки.** Тестовый конструктор,
используется только из `configtest`; асимметрия задокументирована в его
doc-комментарии. Если он когда-нибудь понадобится в проде — сначала
провести публикацию через `setConfig`.

**Узкие интерфейсы в `ui/model` и `ui/dialog`.** Там сосредоточены почти
все 24 обращения к `Config()`. `ui/chat` уже показывает образец
(`ModelConfig`, `CustomAgentConfig`). Это читаемость и инкапсуляция, и
продавать это как уменьшение связности нельзя — линковка не изменится.

---

# Аудит 3 (2026-09-03, `main`, c44e3d612)

Проведён по методу, выведенному из провала предыдущего: читать
потребителей, а не считать метрики. Стандарт приёмки находки — сценарий,
путь с `file:line`, последствие и цитаты кода; формулировки вроде
«выглядит хрупко» отклонялись сразу. Четыре области, три находки, все три
пережили перепроверку и закрыты.

Закрыто: незащищённое чтение вне рабочей папки в `glob`/`grep`/ripgrep
(`db2bac955`), потеря завершения припаркованного треда (`5c73b1498`),
живые обновления вложенных делегирований (`0ebb3d633`).

## Открыто после третьего аудита

### A1 [M] Transient-отказ перезагрузки помечает изменение обработанным

`internal/config/watch.go:98-103`: когда `ReloadFromDisk` возвращает
ошибку, сторож всё равно зовёт `CaptureStalenessSnapshot`, а
`statSnapshot` (`staleness.go:67`) берёт за эталон файл **в текущем виде
на диске** — то есть отвергнутую правку. Изменение помечено увиденным,
повтора не будет.

**Осторожно, здесь легко сделать хуже.** Для невалидного конфига текущее
поведение правильное: без capture `reloadFromDisk` — а он делает HTTP за
моделями — запускался бы каждый интервал опроса. И правку пользователя
это не теряет: исправление меняет файл, mtime сдвигается, изменение
детектируется заново.

Дыра только в transient-случае: таймаут discovery, разовый сбой
`ValidateHooks`, ошибка ФС. Файл валиден, правка нужна, но не применится
никогда. Чинить — классификацией ошибки («конфиг плох» против «попытка
сорвалась»), а не удалением capture.

### A2 [S] Сброс сообщений при завершении не повторяется

`internal/message/store/service.go:414-429`: `Close` выставляет
`s.closed = true` **до** `FlushAll`, а `rearmFlushTimer` (`:567`) при
`closed` гарантированно no-op. Значит любой отказ записи во время
собственного сброса `Close` неповторим. Ошибка доходит до
`app/shutdown.go:321` и логируется, но без указания, какое сообщение
потеряно. Узко — процесс всё равно завершается, — но лог стоит сделать
адресным.

### A3 [S] `Config.AgentOverride` не nil-безопасен

`config.go:810` обращается к `c.Agents` без проверки `c == nil`, тогда
как восемь его соседей в том же файле такую проверку имеют. В проде
недостижимо: стор всегда публикует непустой конфиг. Находкой не
считается — записано как несогласованность, которая ждёт первого
тестового двойника, возвращающего `nil`.

---

# Аудит 4 (2026-09-03, `main`, b0975aeea)

Тем же методом, по областям, которых не касались первые три: `lsp`,
`shell`, `db`, `mcp`, `oauth`, `hooks`, `skills`. Выбор области был не
случаен — оба падения CI после первого пуша пришли именно оттуда.
Четыре находки, три закрыты.

Закрыто: молчаливая порча файла в `applyTextEdit` (`3025624a3`),
`StateNeedsAuth` не чистил сессию и каталог MCP (`54841f0b5`), ложный
комментарий про Windows (`9364e2c87`). Плюс два теста, которые CI нашёл
раньше меня: гоночный тест сторожа скиллов (`a651a4329`) и SIGINT-тест,
неприменимый на Windows (`b0975aeea`).

## Открыто после четвёртого аудита

### B1 [L, не делать наспех] Windows не убивает дерево процессов

`interp.DefaultExecHandler`, который `internal/shell/exec_windows.go`
использует без изменений, не ставит `SysProcAttr` вообще, шлёт сигнал
одному процессу и не задаёт `WaitDelay`. Внуки переживают отмену
команды; `Wait()` может зависнуть, пока внук держит открытым пайп
вывода. На Unix ровно это закрыто через `Setsid` и убийство группы по
отрицательному PID.

Комментарий, обещавший покрытие через `CREATE_NEW_PROCESS_GROUP`,
исправлен на правду (`9364e2c87`). Сама реализация — Job Object с
`JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, назначение процесса при запуске
через `CREATE_SUSPENDED` — **осознанно не сделана**: ни эта машина, ни
локальный прогон её не исполнят, первым исполнением стал бы Windows-
раннер CI, а ошибка в порядке назначения задания проявляется только в
рантайме. Делать отдельной работой, с реальной Windows-машиной.

### B2 [M] Гонка `discard` в пуле соединений `db`

`internal/db/connect.go`: ожидающий в `entry.wait()` получает
`(conn, nil)`, потому что `wait()` возвращает только `entry.err` и
никогда не сверяется с `discard`. Открывающий следом видит `discard` и
закрывает тот же `*sql.DB` — у ожидающего на руках остаётся закрытая
ручка при `err == nil`, а его последующий `Release` сообщает о
несуществующем злоупотреблении.

Срабатывает только после непарного `Release` — то есть это второй баг
поверх злоупотребления, а не достижимый из корректной пары
`Connect`/`Release`. Но исход хуже уже принятого техдолга: там закрытие
детерминировано внутри `Release`, здесь законному ожидающему сообщают
об успехе за мгновение до инвалидации.

## Как читать этот документ дальше

Девять находок из семнадцати проверку не пережили, и все девять были
выведены из метрик: размер файла, счётчики импортов, имена файлов,
отношение комментариев к коду, срабатывания линтера. Всё, что найдено
чтением кода, устояло.

Поэтому: **находку, полученную из метрики, проверяют чтением
потребителей до того, как заводить под неё работу.** Дешёвая проверка
экономит откат, а сама проверка обычно даёт знание ценнее исходной
находки — так выяснилось, что тридцать файлов UI импортируют `proto`
под псевдонимом, что запись креденшела и есть запись конфига, и что в
контракте, который линкует UI, живёт подпроцесс.
