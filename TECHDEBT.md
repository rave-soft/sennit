# Технический долг

Что осталось незакрытым после форка Crush, ребрендинга и удаления внешних
сервисов. Каждый пункт — с причиной и понятным следующим шагом, чтобы к нему
можно было вернуться без повторного расследования.

## Отключённые тесты

### Кросс-процессное обновление OAuth-токена — 4 теста

`internal/config/refresh_singleflight_test.go`

Отключены `InProcessSingleFlight`, `CrossProcessAdopt`,
`StalePeerBorrowsRotatedRefreshToken`, `IgnoresOlderDiskToken`. Два соседних
теста того же файла работают.

Фикстура кладёт провайдера в конфиг на диске и ожидает, что запись он
переживёт. Раньше это выполнялось: провайдер приходил из каталога, который
Crush скачивал с `catwalk.charm.land`. Braid использует только встроенный
каталог, поэтому запись переоценивается как пользовательский провайдер и
отбраковывается при перезагрузке конфига — инстанс, выполнивший обмен,
перезаписывает файл лишь полями учётных данных.

Сама логика обновления токена не менялась. Перевод фикстуры на `copilot`
(провайдер из встроенного каталога) оживляет два теста из четырёх, остальным
мешает потеря полей провайдера при записи.

**Следующий шаг:** разобраться, почему запись токена не сохраняет остальные
поля провайдера. Это стоит проверить не только ради тестов — то же поведение
может ронять пользовательский провайдер после обновления токена.

### Записанные интеграционные кассеты агента — 13 подтестов

`internal/agent/agent_test.go`, `internal/agent/testdata/`

`TestCoderAgent` целиком отключён. Кассеты содержат трафик, записанный Charm
против `hyper.charm.land`, и в телах запросов зашиты полная схема инструментов
и системный промпт того времени — включая имена `crush_info` и `crush_logs` и
старое название продукта. После ребрендинга ни один запрос не совпадёт, а
перезаписать записи без ключа от сервиса Charm невозможно.

**Следующий шаг:** перенаправить `hyperBuilder` в `internal/agent/common_test.go`
на любой OpenAI-совместимый эндпоинт (например, локальную llama.cpp) и
перезаписать кассеты. Тесты и фикстуры оставлены на месте именно ради этого.

## Поведение, требующее проверки

### Провайдер теряет поля при записи токена

Следствие предыдущего пункта, но потенциально задевает пользователей.
Инстанс, обновивший OAuth-токен, судя по всему перезаписывает секцию
провайдера в конфиге только полями учётных данных. Пока каталог провайдеров
скачивался, потерянные `type` и `base_url` восстанавливались из него; теперь
восстанавливать неоткуда.

Затрагивает только провайдеров с OAuth (сейчас это Copilot). Провайдеры с
`api_key` этот путь не проходят.

## Незавершённый ребрендинг

- В комментариях остались упоминания `Charm™` там, где они описывают
  происхождение поля или стиля (`internal/ui/logo/logo.go`,
  `internal/ui/styles/styles.go`). Это факты, а не оформление, поэтому не
  тронуты.
- В `internal/ui/logo` сохранились `hyperLetterforms` и опция `Opts.Hyper` —
  мёртвый код после удаления Hyper.
- Буквы логотипа B, A, I, D нарисованы под ребрендинг и по стилю грубее
  оригинальных C, R, U, S, H.

## Решения, отложенные до подтверждения

### GitHub Copilot

Не удалён. Это провайдер модели, как OpenAI или Anthropic, а не сервис Charm:
удаление лишает бэкенда, а не отвязывает от чужой инфраструктуры. Если решение
— выносить и его, удалять нужно `internal/oauth/copilot`, команды `login` и
`logout`, диалог `internal/ui/dialog/oauth_copilot.go` и ветку обмена токена в
`internal/config/store.go`.

## Ограничения перенесённых определений агентов

- Блоки `permission:` из файлов opencode **не применяются**. Роль, запертая
  там в read-only, в Braid получит всё, что перечислено в её `tools`.
  Ограничивать нужно списком `tools` или секцией `permissions` конфига.
- Поле `model` из чужих файлов игнорируется: понимаются только слоты `large`
  и `small`, а не ссылки вида `opus` или `provider/model`.
- Имена инструментов, которым нет соответствия (например, `WebSearch`),
  молча отбрасываются.
- Делегирование одноуровневое: роль не может вызвать другую роль.

## Мелочи

- Рабочий каталог интеграционных тестов агента — `/tmp/crush-test`: он
  зафиксирован в кассетах и переименован быть не может, пока они не
  перезаписаны.
