site_name: Sennit
site_description: A terminal-first AI coding agent with real multi-agent orchestration.
site_url: https://rave-soft.github.io/sennit/
repo_url: https://github.com/rave-soft/sennit
repo_name: rave-soft/sennit
edit_uri: edit/main/docs/
copyright: FSL-1.1-MIT

docs_dir: docs

theme:
  name: material
  icon:
    repo: fontawesome/brands/github
  features:
    - navigation.instant
    - navigation.tracking
    - navigation.sections
    - navigation.top
    - content.action.edit
    - content.code.copy
    - search.suggest
  palette:
    - media: "(prefers-color-scheme: light)"
      scheme: default
      primary: black
      accent: indigo
      toggle:
        icon: material/weather-night
        name: Switch to dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      primary: black
      accent: indigo
      toggle:
        icon: material/weather-sunny
        name: Switch to light mode

nav:
  - Home: index.md
  - Getting started: getting-started.md
  - Configuration:
      - configuration/index.md
      - sennitrc reference: configuration/sennitrc.md
      - Providers and models: configuration/providers.md
      - Context files: configuration/context.md
      - Permissions: configuration/permissions.md
      - Environment and paths: configuration/environment.md
      - Legacy JSON: configuration/json.md
      - Future work: configuration/future.md
  - Extending:
      - extending/index.md
      - Agents: extending/agents.md
      - Skills: extending/skills.md
      - Commands: extending/commands.md
      - Hooks: extending/hooks.md
      - Hooks, future work: extending/hooks-future.md
      - MCP: extending/mcp.md
      - LSP: extending/lsp.md
  - Concepts:
      - concepts/index.md
      - Steering, tasks and threads: concepts/delegation.md
      - Sessions and data storage: concepts/sessions.md
  - Reference:
      - reference/index.md
      - Command line: reference/cli.md
      - Terminal UI: reference/tui.md
      - Built-in tools: reference/tools.md
  - Troubleshooting: troubleshooting.md
  - Contributing: contributing.md

# Engineering decision records are kept out of the published site. They
# live under docs/ so they sit with the rest of the written material and
# are reviewed alongside it, but they are measurement notes addressed to
# whoever picks the question up next, and they cite plan item IDs ("T15
# r1", the "T0 parallel-tool allow-list") from a working document that has
# since been closed and deleted. A reader arriving at sennit's docs has
# nothing to resolve those against.
#
# exclude_docs rather than not_in_nav: the latter only drops them from the
# navigation, leaving the pages built and reachable by URL, which is the
# same publication with the signpost removed. Either one satisfies
# `mkdocs build --strict`, which fails on a page under docs/ that no nav
# entry mentions — rightly, so that leaving a page out is a decision
# someone writes down instead of an oversight.
exclude_docs: |
  decisions/

markdown_extensions:
  - admonition
  - attr_list
  - github-callouts # renders GitHub-style "> [!NOTE]" blocks as admonitions
  - def_list
  - md_in_html
  - tables
  - toc:
      permalink: true
  - pymdownx.details
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.inlinehilite
  - pymdownx.superfences
  - pymdownx.tabbed:
      alternate_style: true

plugins:
  - search

validation:
  omitted_files: warn
  absolute_links: warn
  unrecognized_links: warn
  anchors: warn
