List every strand: its ID, name, status, branch, and a short summary.

Use this to see what parallel work streams currently exist before deciding
whether to check on one (`strand_status`), send it more work
(`strand_send`), merge it (`strand_merge`), or remove it (`strand_remove`).
Takes no parameters.
