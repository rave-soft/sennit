List every thread: its ID, name, status, branch, and a short summary.

Use this to see what parallel work streams currently exist before deciding
whether to check on one (`thread_status`), send it more work
(`thread_send`), merge it (`thread_merge`), or remove it (`thread_remove`).
Takes no parameters.
