Search file contents with ripgrep using regex (Rust regex syntax) or literal text; returns matching file paths sorted by modification time (max {{ .MaxResults }}); respects .gitignore and .sennitignore. Supports case-insensitive search via case_insensitive. Use glob to filter by filename, not contents.
