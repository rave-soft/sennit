List every background task delegation in this workspace: its ID, status,
and goal.

Use this to see what background work is currently running before checking
on one (`task_result`) or stopping it (`task_cancel`). Takes no
parameters.
